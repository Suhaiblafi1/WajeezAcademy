# قواعدُ العمل في هذا المستودَع

> ملفٌّ قصيرٌ بقصد: ما فيه **قرارات صاحب المنصّة** لا شرحُ الشيفرة. والشرحُ
> يسكن مع ما يشرحه — في رأس الملفّ الذي يفعله.

## القواعدُ القائمة

- **النشرُ والدمجُ تلقائيّان بلا استئذان.** قرارُ صاحب المنصّة (٦ سبتمبر
  ٢٠٢٦)، نسخا لقاعدة «لا دفعَ إلى الإنتاج إلّا بكلمة ارفع» التي كانت قبله.
  فما اكتمل وخضرّت بوّاباتُه يُدمج إلى `main` ويصل الإنتاجَ بلا سؤال.

  **والشرطُ الوحيدُ الباقي: لا يُدمج أحمر.** خضرةُ CI هي الإذن، لا رأيي في
  الشيفرة ولا خضرةُ جهازي — فقد تكرّر في هذه المنصّة أن يمرّ العملُ محلّيّا
  ويسقط في CI، والحكمُ لها.

- **لا اسمَ مدرّب ولا قصّةَ نجاحٍ تُعرض حقيقةً** قبل توثيقها واعتمادِ نشرها
  (`publishApprovedAt`).

- **لا أسرارَ في Git مطلقا** — `.env` و`deploy/.env.production` و
  `storage/private/` و`.pgdata/` مستثناةٌ دائما. ولا يُنقل مفتاحٌ في محادثة.

- **الدفعُ حيٌّ على الإنتاج** — لا يُمَسّ تخزينُ مفاتيحه خارج نافذةِ صيانةٍ
  متّفقٍ عليها. والمشغّلُ اليومَ **تجريبيّ** بأمرِ صاحب المنصّة حتّى يُتحقَّق
  من كلّ شيء.

## أعرافٌ تقنيّةٌ يُبنى عليها

- **الحارسُ يُثبَت سقوطُه.** كلُّ اختبارٍ جديدٍ يُنقض ما يحرسه مرّةً ليُرى
  وهو يسقط، وإلّا فهو زينة. وقد مرّ في هذه الجلسة ثلاثةُ حرّاسٍ خضراءَ
  لأسبابٍ خاطئة (طابقوا نصّا في تعليق، أو اسما جزءا من اسم) — فالفحصُ على
  **البنية** لا على ورودِ حرفٍ في ملفّ.

- **البوّاباتُ خطوطُ أساس** (`scripts/lint-baseline.ts` وأمثالُها): تسقط على
  الدَّين الجديد ولا تجمّد الإصلاح. ولا يُحدَّث خطُّ الأساس ليمرّ الحاجز.

- **قيودُ الحالات مولَّدة** بـ`scripts/status-checks.ts` لا مكتوبةٌ باليد.

- **معجمُ أفعال الأثر** في `src/application/audit/labels.ts`، ويحرسه
  `server/tests/audit/entity-timeline.test.ts`. من أضاف فعلَ أثرٍ شغّل
  `server/tests/audit` معه.

- **لا تُعدَّل ملفّاتٌ ولا تُشغَّل vitest ثانيةٌ وجولةُ الخادم جارية** — فهي
  تتقاسم PostgreSQL المدمجة، والنتيجةُ تفسد بلا أن تقول إنّها فسدت.

- **النشرُ والبنيةُ التحتيّة**: `deploy/` هو المرجع (خادمُ Hetzner: حاويات
  Docker خلف Caddy). و`docs/DEPLOYMENT.md` يشرحه.
