/* ارتفاعُ إطار Calendly — قرارٌ يُفحص، لا سطرٌ في `useState`.

   ═══ العطبُ الذي كُتب له ═══

   الصندوقُ كان يتبع كلَّ رسالةِ ارتفاعٍ صعودا ونزولا، فيقفز في الصفحة ثلاث
   مرّاتٍ في حجزٍ واحد: التقويمُ طويل، ثمّ قائمةُ الأوقات أقصر، ثمّ النموذجُ
   أطول. ومن كان يقرأ ما تحت البطاقة انتُزع من موضعه مرّتَين (شكوى صاحب
   المنصّة، ١٣ سبتمبر ٢٠٢٦: «بوكس ثابت وليس متحرّكا داخليّا»).

   والقاعدةُ: **يكبر ولا يصغر.** يرتفع حين لا يسع المحتوى — فلا تمريرَ داخل
   الإطار ولا قصَّ خطوة — ولا ينزل بعدها فلا يرتجّ ما تحته.

   وهي هنا دالّةً خالصةً لا شرطا في المكوّن، لأنّ شرطا في JSX لا يُفحص إلّا
   بمطابقة نصِّ ملفّ — وهي المطابقةُ التي مرّ منها حرّاسٌ خضرٌ لأسبابٍ خاطئة
   في هذه المنصّة. */

/** أضيقُ ما يُصدَّق وأوسعُه — ما دونهما رسالةٌ مشوَّهةٌ أو مصدرٌ عابث */
const MIN_PX = 320
const MAX_PX = 4000

/** الارتفاعُ يصل نصّا مثل `"1024px"`، ويُقبل الرقمُ كذلك احتياطا */
export function parseFrameHeight(raw: unknown): number | null {
  const px = typeof raw === 'number' ? raw : Number.parseInt(String(raw ?? ''), 10)
  if (!Number.isFinite(px) || px <= MIN_PX || px >= MAX_PX) return null
  return px
}

/** الارتفاعُ التالي: أكبرُ ما بُثّ — و`null` يبقى `null` حتّى تصل رسالةٌ صالحة */
export function nextFrameHeight(prev: number | null, raw: unknown): number | null {
  const px = parseFrameHeight(raw)
  if (px === null) return prev
  return prev === null ? px : Math.max(prev, px)
}
