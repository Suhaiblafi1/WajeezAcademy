import { BarChart3, Compass, Headphones, UserCheck } from "lucide-react";
import { wajeezSkillsStats } from "./trustMetrics";

/* ما يُعطى مع المسار، وما هو مجّانيٌّ للجميع — من مصدرٍ واحد.

   ═══ لماذا صار المصدرُ واحدا والقائمتان مشتقّتَين ═══

   كانت `PATHWAY_ONLY_PERKS` قائمةً واحدةً غرضُها مكتوبٌ في رأسها: «ثلاثةٌ من
   المنظومة لا تُعطى لمن يشتري دورة مفردة — فهي فرقُ الشراءَين».

   وقرّر صاحبُ المنصّة (١٣ سبتمبر ٢٠٢٦) أن تكون ملخّصاتُ الكتب **مجّانا
   للجميع لفترةٍ محدودة**. ووضعُ مجّانيٍّ للجميع في قائمةِ «فرقِ الشراءَين»
   يناقض نفسَه: من قرأها فقَد سببا للشراء بدل أن يكسب.

   وحذفُها من القائمة كان الحلَّ الأسهلَ وأسوأَه: تنتهي الفترةُ المحدودةُ بعد
   شهرَين فيجب أن يتذكّر أحدٌ أن يعيدها — ولا شيءَ يذكّره. فالعلامةُ
   `freeForAllNow` على البند، والقائمتان تُشتقّان منها: **تُطفأ الرايةُ فيعود
   البندُ إلى قائمة المسار وحدَه بلا أن يُكتب سطر**.

   ويحرس تنافيَهما `src/tests/design/perk-lists.test.ts`. */

/** الرقمُ من مصدره الموثَّق لا مكتوبا باليد — يتغيّر هناك فيتغيّر هنا */
const summariesCount =
  wajeezSkillsStats.find((m) => m.key === "book_summaries")?.display_value ?? "";

export interface Perk {
  icon: typeof Headphones;
  t: string;
  d: string;
  /** مجّانيٌّ للجميع الآن — يخرج من قائمة المسار ما دامت الرايةُ مرفوعة */
  freeForAllNow?: true;
  /** إلى أين يذهب من نقر — داخليٌّ فقط، ولا يُخترع رابطٌ خارجيّ */
  href?: string;
  /** شرحٌ يُفتح في الصفحة نفسِها حين لا يكون له وجهة */
  explain?: { titleAr: string; paras: string[] };
}

/* ─────────── ما يُشترى مع المسار ─────────── */
const PATHWAY_PERKS: Perk[] = [
  {
    icon: Headphones,
    t: "ملخصات كتب وجيز الصوتية",
    d: "تسمعها ثم تختبر نفسك فيها — ومجّانا للجميع لفترة محدودة",
    freeForAllNow: true,
    /* ولا رابطَ خارجيّ: جسرُ المكتبة إلى «وجيز مهارات» غيرُ مربوطٍ بعد
       (`application/content/library.ts`)، وسياسةُ التأليف تقول صراحةً «لا
       يُخترع عنوانُ كتابٍ ولا رابط». فالشرحُ في الصفحة، بما نملك إثباتَه. */
    explain: {
      titleAr: "ملخصات كتب وجيز الصوتية",
      paras: [
        `ملخّصاتٌ صوتيّةٌ لكتبٍ مهنيّةٍ من مكتبة «وجيز» — المنصّة الأمّ التي خرجت منها الأكاديمية. ${summariesCount} ملخّص.`,
        "الكتابُ يأخذ منك ثماني ساعات، والملخّصُ يعطيك إطارَه في عشرين دقيقة. فتدخل جلسةَ التدريب وأنت تعرف المفردات، ويصير وقتُ المدرّب للتطبيق لا للتأسيس.",
        "ولا يُترك السماعُ بلا أثر: بعد كلّ ملخّصٍ اختبارٌ قصيرٌ تعرف به ما بقي معك — فلا يكون «سمعتُه» جوابا.",
        "وهي مجّانيّةٌ للجميع لفترةٍ محدودة: بمسارٍ أو بدورةٍ مفردةٍ أو بلا شراءٍ أصلا.",
      ],
    },
  },
  { icon: BarChart3, t: "خريطة مهارات قبل وبعد", d: "مستواك 0–5 في كل مهارة قبل المسار وبعده — بالقياس لا بالانطباع" },
  { icon: UserCheck, t: "مستشار نجاح يرافقك", d: "متابعة أسبوعية ورسالة مباشرة عند أي تعثر" },
];

/* ─────────── ومجّانيٌّ دائما، لا علاقةَ له بشراء ─────────── */
const ALWAYS_FREE: Perk[] = [
  {
    icon: Compass,
    t: "اختبار تحديد الميول — مجانا",
    d: "لطلبة الجامعات والخريجين الجدد · 24 سؤالا في ثلاث دقائق، بلا حساب",
    freeForAllNow: true,
    /* موجودٌ ويعمل منذ البند ٤٠ — لا يُبنى شيءٌ، وإنّما يُدَلُّ عليه.
       ومن أجاب هنا لا يُسأل في التشخيص (`mirror/bridge.ts#mirrorAnsweredIds`)
       — فهو يُقصّر التشخيصَ لاحقا ولا يطيله. */
    href: "/mirror",
  },
];

/** فرقُ الشراءَين وحدَه — وما رُفعت رايتُه مجّانا ليس منه */
export const PATHWAY_ONLY_PERKS: Perk[] = PATHWAY_PERKS.filter((p) => !p.freeForAllNow);

/** ما يناله كلُّ أحدٍ الآن — بشراءٍ أو بغيره */
export const FREE_FOR_ALL_NOW: Perk[] = [
  ...ALWAYS_FREE,
  ...PATHWAY_PERKS.filter((p) => p.freeForAllNow),
];
