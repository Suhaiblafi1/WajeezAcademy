/* عناوينُ الأكاديميّة — واحدٌ لكلّ غاية، ومصدرٌ واحدٌ لا يُكتب بالأيدي.

   ── لماذا التقسيم ──

   كان عنوانٌ واحدٌ يخدم كلَّ شيء: المُرسِلَ الآليّ، ودعواتِ التقويم، وصفحةَ
   التواصل، وطلباتِ البيانات، ونزاعَ الشروط، وطلبَ الاسترداد. وثمنُ ذلك ثلاثة:
   لا يُعرف من نصِّ العنوان لماذا وصلت الرسالة، ولا تُوجَّه إلى من يعنيه أمرُها،
   ولا يُقاس شيء — فطلبُ استردادٍ وسؤالُ زائرٍ في صندوقٍ واحد.

   ── والقاعدةُ التي تحكم هذا الملفّ: العناوينُ رخيصة، والصناديقُ ليست كذلك ──

   خمسةُ عناوينَ لا تعني خمسةَ صناديقَ يفتحها أحد. **تُنشأ أسماءً مستعارةً
   (aliases) تصبّ في صندوقٍ واحدٍ أو اثنين**، ويبقى الظاهرُ للمتعلّم والجهةِ
   الرسميّة عنوانا يخصّ غايته. والعكسُ هو الخطأ الشائع: عنوانٌ على صفحةٍ
   قانونيّةٍ لا يقرؤه أحد أسوأُ من عنوانٍ عامٍّ يُقرأ.

   ── والمُرسِلُ لا يُردّ عليه، والردُّ يُوجَّه ──

   الرسائلُ الآليّة تخرج من `no-reply@` — وهو صادقٌ في اسمه: لا أحدَ يقرأ ما
   يصله. لكنّ من يضغط «ردّ» على رسالةِ توثيقٍ إنسانٌ يحتاج جوابا، فتُضبط
   ترويسةُ `Reply-To` على عنوان الدعم. فيُجيبه إنسانٌ بلا أن يوهمه المُرسِلُ
   أنّه صندوقٌ مراقَب.

   ── والنطاق ──

   كلُّها على نطاق الموقع، **فتوثيقٌ واحدٌ في Resend يغطّيها جميعا** — وSPF
   وDKIM تُضبط للنطاق لا للعنوان.

   وللخادم نسخةٌ ثانيةٌ لازمة (`server/services/integrations.service.ts`) لأنّه
   لا يستورد من `src/`. ويحرس تطابقَهما `src/tests/academy-email.test.ts`، ومعه
   حارسٌ يمنع أن يُكتب عنوانٌ حرفا في أيّ ملفٍّ آخر. */

/** نطاقُ البريد — يُغيَّر هنا وحدَه، فتتبعه العناوينُ كلُّها */
export const ACADEMY_EMAIL_DOMAIN = 'wajeezacademy.com'

const at = (local: string) => `${local}@${ACADEMY_EMAIL_DOMAIN}`

/** الاسمُ الظاهرُ في صندوق الوارد */
export const ACADEMY_EMAIL_NAME = 'أكاديمية وجيز'

export const ACADEMY_EMAILS = {
  /** المُرسِلُ الآليّ — توثيقُ البريد والاستعادةُ والفواتيرُ والتذكيرات.
      لا يُقرأ ما يصله، ولذلك يُصرَّح باسمه، ويُوجَّه الردُّ إلى الدعم. */
  noReply: at('no-reply'),

  /** الدعمُ العامّ — صفحةُ التواصل، وسؤالُ الزائر، و`Reply-To` لكلّ رسالةٍ آليّة */
  support: at('support'),

  /** الخصوصيّةُ وطلباتُ البيانات — نسخةٌ أو تصحيحٌ أو حذف */
  privacy: at('privacy'),

  /** الشروطُ والنزاعات */
  legal: at('legal'),

  /** الماليّة — الاستردادُ والفواتير */
  billing: at('billing'),

  /** منظِّمُ دعوات التقويم في ملفّات `.ics` — جهةٌ لا شخص */
  calendar: at('calendar'),
} as const

/** العنوانُ العامُّ حين لا تكون الغايةُ محدَّدة */
export const ACADEMY_EMAIL = ACADEMY_EMAILS.support
