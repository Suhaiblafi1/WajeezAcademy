/* أتعابُ المدرّبين ومستحقّاتُهم — شاشةٌ بابُها من صلاحيّتها.

   ── العطبُ ──

   محرِّرُ قواعد الأتعاب (`TrainerPayouts`) كان لسانا في **«طلبات انضمام
   المدربين»**، وبابُ تلك الشاشة `trainer.applications.view`. والأتعابُ
   محروسةٌ بـ`trainer.compensation.manage`. والصلاحيّتان لا تلتقيان إلّا
   في `super_admin`:

     · `trainer.applications.view` — المديرُ الأكاديميّ والمنسّقُ ومديرُ
       العمليّات
     · `trainer.compensation.manage` — **المالية**

   فحسابُ المالية على `/admin/trainers` يُردّ: «هذا الإجراء يتطلّب صلاحيّة
   «عرض طلبات انضمام المدربين»» — وهو بابٌ لا شأنَ له بالأتعاب أصلا،
   ويفتح على سيرٍ ذاتيّةٍ ودوافعَ وبياناتٍ شخصيّةٍ لمتقدّمين. وحسابُ المدير
   الأكاديميّ يبلغ اللسانَ ثمّ يُردّ عند أوّل فعل.

   **ولمَ هو خطيرٌ لا مزعجٌ فحسب:** بلا قاعدةِ أتعابٍ لا يُولَّد كشفٌ
   إطلاقا — `computeCohort` يرمي `no_rule`. فهذا البابُ المسدودُ هو بعينه
   ما يُبقي «مستحقّاتي» صفرا عند كلّ مدرّب.

   ── والجوابُ نقلُ الباب لا توسيعُ المفتاح ──

   منحُ الماليّةِ `trainer.applications.view` يفتح لها ملفّاتِ المتقدّمين
   كلَّها لتضبط رقما. فصارت للأتعاب شاشتُها، وبابُها في القائمة محروسٌ
   بـ`trainer.compensation.manage` وحدَها — من يملك الصلاحيّةَ يرى الباب،
   ومن لا يملكها لا يراه ولا يُردّ عنده. */

import AdminLayout from "./AdminLayout";
import { TrainerPayouts } from "./TrainerOps";

export default function TrainerCompensation() {
  return (
    <AdminLayout title="أتعابُ المدرّبين ومستحقّاتُهم">
      <TrainerPayouts />
    </AdminLayout>
  );
}
