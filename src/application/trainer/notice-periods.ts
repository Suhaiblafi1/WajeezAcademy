/* مُهَلُ الإشعار — أرقامٌ في موضعٍ واحد، يقرؤها متنُ العقد وتقرؤها الشاشات.

   ── ولماذا ملفٌّ لرقمين ──

   العقدُ يقول «ثلاثين يوما»، والشاشةُ تعدّ الأيّامَ المتبقّية، والتذكيرُ يخرج
   قبل الأجل. فثلاثةُ مواضعَ تقول العددَ نفسَه — ومتى كُتب حرفا في كلٍّ منها
   افترقت يوما: يُغيَّر في العقد وتبقى الشاشةُ تعدّ بالقديم، فيُقال للمدرّب
   رقمٌ ويُحاسَب بغيره.

   ── وهي نصٌّ لا إنفاذٌ — في هذه المرحلة ──

   لا تردُّ هذه الثوابتُ اليومَ إلغاءً وقع داخل النافذة. فالإنفاذُ يمسّ إلغاءَ
   الشعب وطلباتِ نقل الجلسات ومسارَ الرحيل في ثلاث خدمات، وله بندُه. وقيمتُها
   الآن أنّ ما يُطبَع في العقد وما يُعرض في الشاشة من مصدرٍ واحد — فيوم يُنفَّذ
   الإنفاذُ يجد الرقمَ حيث يتوقّعه ولا يخترع رابعا.

   والأرقامُ اختيرت من المدى الذي ذكره صاحبُ المنصّة، وفي كلّ موضعٍ اختير
   **الطرفُ الأرفقُ بالمدرّب** — لأنّ العقدَ الذي يُقرأ منصفا يُنفَّذ، والذي
   يُقرأ مجحفا تُخفَّض بنودُه كلُّها لا البندُ المجحفُ وحدَه. */

/** اعتذارُ المدرّب عن شعبةٍ قبِلها — «ولا يعتذر إلا قبل شهر على الأقل» */
export const TRAINER_WITHDRAWAL_NOTICE_DAYS = 30

/** إلغاءُ الأكاديميّة شعبةً — «خمسة أيام أو سبعة أيام»، فسبعة */
export const ACADEMY_CANCELLATION_NOTICE_DAYS = 7

/** أقلُّ مهلةٍ لطلب تغيير موعد جلسة، بالساعات */
export const RESCHEDULE_NOTICE_HOURS = 72

/** أكثرُ ما يُقبل من تغييرات المواعيد في الشعبة الواحدة — بعدها يُبحث الأمر */
export const RESCHEDULE_MAX_PER_COHORT = 3

/** مهلةُ الردّ على عرض الإسناد — وانقضاؤها بلا ردٍّ ردٌّ */
export const ASSIGNMENT_OFFER_RESPONSE_DAYS = 5

/** أجلُ إعداد الدورة، يبدأ من **قبول** العرض لا من إسناده.

    «على الأقل ثلاثة أيام إلى خمسة أيام لكل دورة — لن نستعجل أكثر». فخمسةٌ
    افتراضا، وثلاثةٌ أقلُّ ما يُقبل، ولا حدَّ أعلى: من أراد شهرا فله. */
export const COURSE_PREP_DEFAULT_DAYS = 5
export const COURSE_PREP_MIN_DAYS = 3

/** عمرُ رابط التوقيع بالأيّام — يُجدَّد إن انقضى ولا يُنسَخ */
export const CONTRACT_SIGNING_LINK_DAYS = 14

/** مُهَلُ صرف المستحقّات — تُقاس من انتهاء الشهر الذي اكتملت فيه الشعبة.

    ── ولمَ من الشهر لا من يوم الاكتمال ──

    الكشفُ يُحرَّر بالفترة لا بالشعبة: `TrainerPayout.period` نصٌّ كـ`2026-08`
    يجمع ما اكتمل في ذلك الشهر. فأجلٌ يبدأ من يوم اكتمال كلّ شعبةٍ يعطي
    لكشفٍ واحدٍ عدّةَ آجالٍ مختلفة — وهو وعدٌ لا يُقاس فلا يُوفى.

    ── وسقفٌ يُقاس بما هو مسجَّلٌ اليوم ──

    `period` و`paidAt` مكتوبان في الجدول أصلا، فسؤالُ «أوُفِّي بالسقف؟» يُجاب
    بعد سنةٍ بلا عمودٍ جديد. أمّا يومُ الاعتماد فغيرُ مسجَّل (`approvedBy`
    وحدَه هو الموجود) — فالمُهلتان الأوليان وصفٌ للمسار، والسقفُ وحدَه هو
    الالتزامُ الذي يُحاسَب عليه.

    والثلاثةُ مترابطة: السقفُ لا يقلّ عن مجموع الأوليين، ويحرسه اختبار. */
export const PAYOUT_APPROVAL_DAYS = 15
export const PAYOUT_TRANSFER_DAYS = 30
export const PAYOUT_OUTER_DAYS = 45

/** الجملةُ التي تُعرَض للمدرّب في «مستحقّاتي» — من الثوابت نفسِها لا نصّا يُكتب.

    وهي ليست تكرارا للبند 4-2: ذاك نصٌّ قانونيٌّ مجمَّدٌ في وثيقةٍ وُقّعت، وهذه
    جملةٌ تُقرأ في شاشة. والمشتركُ بينهما الأرقامُ وحدَها — ومتى كُتبت في
    الشاشة حرفا افترقت عن العقد عند أوّل تعديل، فيُقرأ رقمٌ ويُحاسَب بغيره. */
export function payoutTimingNoteAr(): string {
  return `ويعتمد كشف الشهر خلال ${PAYOUT_APPROVAL_DAYS} يوما من انتهائه، ويصرف خلال `
    + `${PAYOUT_TRANSFER_DAYS} يوما من اعتماده، ولا يتجاوز الصرف ${PAYOUT_OUTER_DAYS} يوما `
    + `من انتهاء الشهر — وهي المدد المكتوبة في البند 4-2 من عقدك.`
}

/** أقلُّ بدلِ تحضيرٍ يُذكر في العقد حين تُلغي الأكاديميّةُ داخل النافذة.

    ولمَ حدٌّ أدنى مقطوع: الأساسُ بعدد المتعلّمين يجعل أتعابَ شعبةٍ أُلغيت قبل
    أن تبدأ دالّةً في مقاعدَ لم تُحجَز — أي صفرا. وبدلٌ قيمتُه صفرٌ ليس بدلا،
    والبندُ كلُّه يصير زينةً تُقرأ علينا. */
export const ACADEMY_LATE_CANCELLATION_FLOOR_USD = 50
