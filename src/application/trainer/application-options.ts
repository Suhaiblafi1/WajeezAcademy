/* خياراتُ طلب الانضمام المشتركة بين النموذج والخادم ولوحة الإدارة.

   مصدرٌ واحد للقيم وأسمائها: القيمةُ تُخزَّن والاسمُ يُقرأ، ولو تفرّقا في
   ثلاثة ملفّات لاختلف ما يراه المتقدّم عمّا يراه المراجع عمّا يفحصه
   الخادم. والوحدةُ نقيّة — لا React ولا Prisma — فتُستورد من الطرفين. */

/** مواسمُ التدريب — أربعةُ أرباعٍ تبدأ بنوفمبر لا بيناير، لأنّ موسمَ
    التدريب العربيّ يبدأ بعد الصيف ويقف في رمضان والصيف لا عند رأس السنة. */
export const TRAINING_SEASONS = [
  { value: 'nov_jan', label: 'موسم الشتاء', months: 'نوفمبر – يناير', monthNums: '١١ – ١' },
  { value: 'feb_apr', label: 'موسم الربيع', months: 'فبراير – أبريل', monthNums: '٢ – ٤' },
  { value: 'may_jul', label: 'موسم الصيف', months: 'مايو – يوليو', monthNums: '٥ – ٧' },
  { value: 'aug_oct', label: 'موسم الخريف', months: 'أغسطس – أكتوبر', monthNums: '٨ – ١٠' },
] as const

export type TrainingSeason = (typeof TRAINING_SEASONS)[number]['value']
export const TRAINING_SEASON_VALUES = TRAINING_SEASONS.map((s) => s.value) as [TrainingSeason, ...TrainingSeason[]]

/** الاسمُ الكامل كما يُقرأ في الملفّ والبريد: «موسم الشتاء (نوفمبر – يناير)» */
export function seasonLabel(value: string): string {
  const s = TRAINING_SEASONS.find((x) => x.value === value)
  return s ? `${s.label} (${s.months})` : value
}

/* ═══ أوصافُ الخبرة والعمل والنمط — بيتُها هنا لا في صفحة النموذج ═══

   كانت هذه القوائمُ في `src/pages/join-trainer/options.ts`: يقرؤها النموذجُ
   وحدَه، والقاعدةُ تحفظ قيمتَها (`4-7`, `own_business`, `remote`) لا اسمَها.
   فلمّا صار **ملفُّ المتقدّم** يُبنى في الخادم ويُرسَل PDF إلى لجنة
   المراجعة، احتاج الخادمُ الأسماءَ نفسَها — ولا سبيلَ له إلى ملفِّ صفحة.

   والبديلُ نسخُها هناك: فيقرأ المتقدّمُ «٤–٧ سنوات» ويقرأ المراجعُ عنه
   شيئا آخرَ بعد أوّل تعديل. فنُقلت إلى الوحدة المشتركة — وهي وحدةٌ نقيّةٌ
   بلا React ولا Prisma، تُستورد من الطرفين — والصفحةُ تصدّرها كما كانت. */

/** سنواتُ الخبرة في المجال — القيمةُ تُخزَّن والاسمُ يُقرأ */
export const DOMAIN_YEARS = [
  { value: '1-3', label: '١–٣ سنوات' },
  { value: '4-7', label: '٤–٧ سنوات' },
  { value: '8-12', label: '٨–١٢ سنة' },
  { value: '12+', label: 'أكثر من ١٢ سنة' },
]

/** خبرةُ التدريب — منفصلةٌ عن خبرة المجال: يُتقن مجالَه ولم يدرّب بعد */
export const TRAINING_YEARS = [
  { value: 'none', label: 'لم أدرّب بعد — لكني أتقن مجالي' },
  { value: 'informal', label: 'تدريب غير رسمي (زملاء / فريقي)' },
  { value: 'workshops', label: 'ورش ودورات قصيرة' },
  { value: 'formal_teaching', label: 'تدريب منهجي معتاد (دورات/شعب)' },
]

export const EMPLOYMENT_STATUS = [
  { value: 'employed', label: 'موظف — أعمل لدى جهة' },
  { value: 'own_business', label: 'لدي عملي الخاص' },
  { value: 'full_time_training', label: 'متفرغ للتدريب' },
]

/** نمطُ التدريب — كان مكتوبا في الوسم نفسِه (`<option value="remote">`) */
export const DELIVERY_MODES = [
  { value: 'remote', label: 'عن بعد' },
  { value: 'in_person', label: 'حضوري' },
  { value: 'both', label: 'عن بعد وحضوري' },
]

/** وقتُ اليوم — اليومُ وحدَه لا يقول متى هو متفرّغ فيه */
export const PERIODS = [
  { value: 'morning', label: 'صباحي' },
  { value: 'evening', label: 'مسائي' },
] as const

/** اسمُ القيمة من قائمتها — وما لا اسمَ له يُعرض كما هو لا يُبتلع */
export function labelOf(list: readonly { value: string; label: string }[], value: string | null | undefined): string {
  if (!value) return ''
  return list.find((x) => x.value === value)?.label ?? value
}

/** كيف نتواصل معه للاجتماع التعريفيّ — أربعُ قنوات، واحدةٌ تُختار */
export const CONTACT_CHANNELS = [
  { value: 'phone', label: 'مكالمة هاتفية', needsPhone: true, needsAltEmail: false },
  { value: 'whatsapp', label: 'واتساب', needsPhone: true, needsAltEmail: false },
  { value: 'email', label: 'البريد الإلكتروني المسجّل', needsPhone: false, needsAltEmail: false },
  { value: 'other_email', label: 'بريد إلكتروني آخر', needsPhone: false, needsAltEmail: true },
] as const

export type ContactChannel = (typeof CONTACT_CHANNELS)[number]['value']
export const CONTACT_CHANNEL_VALUES = CONTACT_CHANNELS.map((c) => c.value) as [ContactChannel, ...ContactChannel[]]

export function contactChannelLabel(value: string): string {
  return CONTACT_CHANNELS.find((c) => c.value === value)?.label ?? value
}

/* ═══════════ حجزُ المقابلة — قرارُ صاحب المنصّة (٦ سبتمبر ٢٠٢٦) ═══════════

   المقابلةُ تجري **خارج** المنصّة منذ صار الاعتمادُ بنقرةٍ واحدة. وكانت شاشةُ
   «وصل طلبك» تَعِد المتقدّمَ وعدا سلبيّا: «نتواصل معك… لتحديد موعد» — فيقف
   ينتظر مكالمةً قد تتأخّر، ولا شيءَ بيده. فصار الموعدُ يُحجَز بيده هو.

   ولماذا أداةٌ خارجيّة لا شاشةٌ عندنا: جدولةُ المواعيد المتاحة تحتاج تقويمَ
   المُقابِل وحجبَ ما حُجز ودعوةَ Zoom ورسالةَ تأكيد — وثلاثتُها لا تملكها
   المنصّةُ اليوم (لا قناةَ بريدٍ فعّالة أصلا). فتُستعمل الأداةُ لما تحسنه،
   ولا يُبنى نصفُ مُجدوِلٍ لا يُرسل شيئا.

   ويُضمَّن الرابطُ داخل الصفحة مع مخرجٍ يفتحه في لسانٍ جديد. وسياسةُ المحتوى
   في كلّ مضيفٍ تسمح بإطار Calendly وحدَه، ولا تسمح بسكربتاته في نطاقنا. */
export const TRAINER_INTERVIEW = {
  /** يُغيَّر من هنا وحدَه — تقرؤه الشاشتان معا */
  url: 'https://calendly.com/hadeel-7/wajeez-academy',
  minutes: 45,
  platformAr: 'اجتماع مرئي',
  /* ═══ اسمُه عند المتقدّم — كلمةٌ واحدةٌ في كلّ ما يقرؤه ═══

     كان يُسمّى في شاشاته ثلاثةَ أسماء: «مقابلة» و«الاجتماع التعريفيّ» و«لقاء
     التعارف». وثلاثةُ أسماءٍ لشيءٍ واحدٍ تُقرأ ثلاثةَ أشياء — فيظنّ من حجز
     «لقاء تعارف» أنّ «المقابلة» موعدٌ آخرُ ينتظره.

     وقرارُ صاحب المنصّة (١٨ سبتمبر ٢٠٢٦): «لقاء التعارف» فيما يقرؤه المتقدّم.
     و«المقابلة» تبقى في شاشات الإدارة — هناك صفٌّ في جدولٍ ونتيجةٌ تُسجَّل،
     لا دعوةٌ تُقرأ. */
  labelAr: 'لقاء التعارف',
} as const

/* ═══════════ وقفُ الحجز المؤقّت — قرارُ صاحب المنصّة (٢٠ سبتمبر ٢٠٢٦) ═══════════

   كثر المتقدّمون حتّى امتلأ تقويمُ المُقابِل: «امتلأت شاشتي بالمواعيد، ولا
   توجد أوقاتٌ متاحةٌ هذا الشهر».

   ═══ والعطبُ الذي يزيله هذا المفتاح ═══

   تقويمُ Calendly حين تنفد أوقاتُه **لا يقول إنّها نفدت**: يعرض شهرا بلا
   يومٍ قابلٍ للنقر. فيقلّب المتقدّمُ الأسابيعَ، ثمّ يظنّ العطبَ في متصفّحه أو
   فينا، ثمّ يغلق الصفحة — وطلبُه وصلنا كاملا وهو لا يدري. فالصمتُ هنا أسوأُ
   من المنع: من مُنع عرف، ومن قلّب شهرا فارغا لم يعرف شيئا.

   فيُقال له صراحةً في موضع التقويم نفسِه: طلبك وصل، وشكرا لك، والحجزُ موقوفٌ
   لامتلاء المواعيد، ويعود إليك الرابطُ في الشهر القادم.

   ═══ ولماذا مفتاحٌ يُطفأ لا تضمينٌ يُحذف ═══

   الوقفُ مؤقّتٌ بشهرٍ واحد. ومن حذف التضمينَ اليومَ أعاد بناءَه في أكتوبر —
   وما يُبنى مرّتين يُبنى الثانيةَ ناقصا: تُنسى سياسةُ المحتوى، أو قياسُ
   الارتفاع، أو التقاطُ حدث الحجز. فالتقويمُ يبقى كما هو ولا يُعرض ما دام
   `active`، ويومَ تُفتح المواعيد يُطفأ هذا وحدَه فيعود كلُّ شيء.

   ═══ وموضعُه هنا لا في البطاقة ═══

   يقرؤه ثلاثةٌ معا: بطاقةُ الحجز، وشاشةُ ما بعد الإرسال، وشرحُ حالة «مقدَّم»
   أدناه. ولو كُتب في البطاقة وحدَها لبقيت الشاشتان تَعِدان بتقويمٍ لا يُعرض. */
export const INTERVIEW_BOOKING_PAUSE = {
  /** يُطفأ وحدَه يومَ تُفتح المواعيد — ولا يُحذف معه شيء */
  active: true,
  /** الشهرُ الذي امتلأ — يُسمّى في الشاشة فلا يُقرأ الوقفُ أبديّا */
  monthAr: 'سبتمبر',
  /** الشهرُ الذي يعود فيه رابطُ الحجز — تقرؤه الشاشاتُ الثلاث */
  resumeMonthAr: 'أكتوبر ٢٠٢٦',
} as const

/** الرابطُ معبَّأً سلفا باسم المتقدّم وبريده ورقم طلبه.

    ولماذا التعبئة: من يفتح صفحةَ الحجز بعد نموذجٍ من أربعة أقسام لا يكتب
    اسمَه وبريدَه مرّةً ثالثة. و`a1` أوّلُ سؤالٍ مخصّصٍ في Calendly — يُملأ
    برقم الطلب إن وُجد السؤال، ويُتجاهَل بلا ضررٍ إن لم يوجد. */
export function trainerInterviewUrl(
  input: { name?: string; email?: string; reference?: string; guests?: string },
  /* الأصلُ البديلُ من شاشة التكاملات — و`TRAINER_INTERVIEW.url` حين لا بديل.
     ويصل مطبَّعا بلا معاملات، فإلحاقُ `?` أدناه يبقى سليما. */
  base: string = TRAINER_INTERVIEW.url,
): string {
  const q = new URLSearchParams()
  if (input.name?.trim()) q.set('name', input.name.trim())
  if (input.email?.trim()) q.set('email', input.email.trim())
  /* ═══ الحاضرون المضافون تلقائيا ═══

     المقابلةُ بين المتقدّم والمضيف وحدَهما، فمن أراد حضورَها من الإدارة لزمه
     أن يُضاف يدويّا في كلّ موعد. و`guests` معامَلٌ يقبله Calendly فيضيفهم بلا
     لمسة — ⚠ بشرط أن يكون «Invitees can add guests» مفعَّلا في نوع الحدث،
     وإلّا تجاهله صامتا. */
  if (input.guests?.trim()) q.set('guests', input.guests.trim())
  if (input.reference?.trim()) {
    const reference = input.reference.trim()
    q.set('a1', reference)
    /* لا يعتمد ربطُ webhook على سؤالٍ مخصّص قد يُحذَف من Calendly: معرّفُ
       الطلب يسافر كذلك في حقل التتبّع الرسميّ ويعود في جسم الحدث. */
    q.set('utm_source', 'wajeezacademy')
    q.set('utm_medium', 'trainer_application')
    q.set('utm_campaign', 'trainer_interview')
    q.set('utm_content', reference)
  }
  const s = q.toString()
  return s ? `${base}?${s}` : base
}

/** حالاتُ الطلب كما تُقال لصاحبه — لا كما تُقال للمراجع.
    الاسمُ قصير، والشرحُ يقول ما يجري الآن وما الذي يليه. */
export const APPLICANT_STATUS: Record<string, { label: string; explain: string; tone: 'neutral' | 'progress' | 'good' | 'warn' | 'bad' }> = {
  draft: {
    label: 'لم يُكمَل بعد',
    explain: 'حفظنا القسم الأول من طلبك. أكمل مستنداتك ووسيلة التواصل ليصل الطلب إلى فريق المراجعة.',
    tone: 'warn',
  },
  email_verification_pending: {
    label: 'بانتظار تأكيد البريد',
    explain: 'افتح رابط التأكيد الذي أرسلناه إلى بريدك ليصبح طلبك مُقدَّما رسميا.',
    tone: 'warn',
  },
  submitted: {
    label: 'وصل طلبك — بانتظار المراجعة',
    /* ═══ والشرحُ يتبع المفتاح — وإلّا وعد بزرٍّ لا يُعرض ═══

       هذان السطران يَعِدان بـ«الزرّ أدناه»، والبطاقةُ تحته تعرض إشعارَ الوقف
       ما دام `INTERVIEW_BOOKING_PAUSE.active`. فمن قرأهما بحث عن زرٍّ ليس
       هناك — وهو العطبُ نفسُه الذي يزيله الإشعار. */
    explain: INTERVIEW_BOOKING_PAUSE.active
      ? `طلبك كاملٌ عند فريقنا ويُقرأ الآن. وحجزُ ${TRAINER_INTERVIEW.labelAr} موقوفٌ مؤقّتا لامتلاء المواعيد — يصلك رابطُ الحجز في ${INTERVIEW_BOOKING_PAUSE.resumeMonthAr}.`
      : 'طلبك كامل عند فريقنا، ولم يبقَ إلّا حجزُ موعدِ لقاء التعارف من الزرّ أدناه — ونقرأ طلبك قبله.',
    tone: 'progress',
  },
  under_review: {
    label: 'قيد المراجعة',
    explain: 'يقرأ فريقنا الأكاديمي طلبك ومستنداتك الآن.',
    tone: 'progress',
  },
  information_requested: {
    label: 'نحتاج معلومات إضافية',
    explain: 'راجع بريدك: طلبنا منك استكمال شيء في ملفك، وبعده يعود طلبك إلى المراجعة.',
    tone: 'warn',
  },
  shortlisted: {
    label: 'اختيار أولي',
    explain: INTERVIEW_BOOKING_PAUSE.active
      ? `اجتاز طلبك الفرز الأوّليّ، والخطوةُ التالية ${TRAINER_INTERVIEW.labelAr}. وحجزُ المواعيد موقوفٌ مؤقّتا لامتلائها — يصلك رابطُ الحجز في ${INTERVIEW_BOOKING_PAUSE.resumeMonthAr}.`
      : 'اجتاز طلبك الفرز الأوّليّ. الخطوةُ التالية لقاءُ تعارفٍ قصير — احجز موعدَه من الزرّ أدناه.',
    tone: 'progress',
  },
  interview_scheduled: {
    label: 'موعدُك محجوز',
    explain: 'حُجز موعدُ لقاء التعارف. راجع بريدك لتفاصيله — ومنه تغيّره أو تلغيه إن لزم.',
    tone: 'progress',
  },
  demo_requested: {
    label: 'بانتظار الدرس التجريبي',
    explain: 'نطلب منك درسا تجريبيا قصيرا تقيّمه لجنتنا الأكاديمية.',
    tone: 'progress',
  },
  academic_review: {
    label: 'مراجعة أكاديمية نهائية',
    explain: 'تكتب اللجنة الأكاديمية تقييمها النهائي بعد لقاء التعارف والدرس التجريبي.',
    tone: 'progress',
  },
  /* ═══ ولا يُبشَّر أحدٌ بقبولٍ لم يقع (٢٠ سبتمبر ٢٠٢٦) ═══

     كانت تقول له «قُبل طلبك مبدئيا» بنبرةٍ خضراء. وهذه الحالةُ صارت طورَ
     **التجهيز الداخليّ**: تُضبط فيها أتعابُه وتُسنَد دوراتُه ويُركَّب عقدُه،
     وقد تنتهي بردٍّ لا بقبول. فمن قرأ «قُبلتَ» ثمّ رُدّ بعد أسبوعَين قُرئ
     عليه وعدٌ نقضناه نحن، لا قرارٌ تغيّر.

     قرارُ صاحب المنصّة: لا يعلم بشيءٍ حتّى يُعتمَد اعتمادا كاملا. والنصُّ
     صادقٌ بلا وعد: يقول أين طلبُه، ويُنبّه أنّ شيئا قد يصله — فالعقدُ يُرسَل
     من هذا الطور فعلا، والصمتُ التامُّ عنه يجعل وصولَه مفاجأةً لا خبرا. */
  conditionally_approved: {
    label: 'مراجعةٌ نهائيّة',
    explain: 'طلبك في مراجعته النهائية. قد يصلك منّا ما يلزم استكمالُه أو توقيعُه — ويصلك القرار حين تكتمل.',
    tone: 'progress',
  },
  contract_pending: {
    label: 'العقد قيد التوقيع',
    explain: 'أرسلنا لك العقد. بعد توقيعه تبدأ تهيئة انضمامك.',
    tone: 'good',
  },
  onboarding: {
    label: 'تهيئة الانضمام',
    explain: 'نجهّز حسابك وموادك. عند اكتمالها تُفتح لك بوابة المدربين من هذا الحساب نفسه.',
    tone: 'good',
  },
  active: {
    label: 'مدرب نشط',
    explain: 'اكتمل اعتمادك. بوابة المدربين مفتوحة لك من هذا الحساب.',
    tone: 'good',
  },
  waitlisted: {
    label: 'قائمة الانتظار',
    explain: 'طلبك مقبول للانتظار: نعود إليه حين تُفتح شعبة في تخصصك.',
    tone: 'neutral',
  },
  rejected: {
    label: 'اعتذرنا هذه المرة',
    /* ═══ ولا مدّةَ انتظارٍ تُوعَد (١٩ سبتمبر ٢٠٢٦) ═══

       كان يُقال «يمكنك التقديم مجددا بعد ستة أشهر». ولا شيءَ في الشيفرة
       يقيسها: `submitPhase1` تردّ من له طلبٌ **حيّ** وحدَه، و`rejected`
       حالةٌ نهائيّة — فمن رُدّ يُقبل طلبُه الجديدُ في الغد. فكان النصُّ
       يَعِد بحاجزٍ لا وجود له، ويصرف عنّا من كان يعود بعد شهر.

       وقرارُ صاحب المنصّة أن تُحذف المدّة من الموضعَين معا: هنا وفي رسالة
       الاعتذار. ويحرسه `src/tests/trainer-decision-mail.test.ts`. */
    explain: 'لم نستطع المضيّ في طلبك حاليا. ولك أن تتقدّم من جديد حين يتغيّر ما تعرضه — بخبرةٍ أو أدلّةٍ جديدة.',
    tone: 'bad',
  },
  withdrawn: {
    label: 'مسحوب',
    explain: 'سحبتَ طلبك. أهلا بك متى ما عدت — يمكنك التقديم من جديد.',
    tone: 'neutral',
  },
  suspended: {
    label: 'موقوف',
    explain: 'حسابك كمدرب موقوف حاليا. تواصل مع الإدارة للتفاصيل.',
    tone: 'bad',
  },
}

/* ═══════════ تسجيلاتُ دوراته — سؤالٌ وجوابُه يفتح سؤالا ═══════════

   قرارُ صاحب المنصّة (٢٠ سبتمبر ٢٠٢٦): يُسأل المتقدّمُ هل لديه تسجيلاتٌ
   جاهزةٌ لدوراته. فإن قال لا، سُئل هل يرغب في تسجيلها — «لأنّ المحتوى الأفضل
   يكون تسجيلاتٍ مسجّلةً وحلقاتٍ مباشرةً مع الطلاب».

   ═══ ولماذا ثلاثةُ أحوالٍ لا اثنان ═══

   الجوابُ منطقيٌّ في القاعدة، **وثالثُه أن لا جواب**: `null`. ولو كان مربّعا
   يُؤشَّر (كما `hasAccreditation`) لَما فُرّق بين «قال لا» و«لم يصل إلى
   السؤال بعد» — والفرقُ هو كلُّ شيءٍ هنا، إذ السؤالُ الثاني لا يُعرض إلّا
   على من قال لا. فمربّعٌ فارغٌ يفتح على كلّ متقدّمٍ سؤالا لم يُسأله أحد.

   ولهذا يُختار الجوابُ اختيارا صريحا: «نعم» أو «لا»، ولا شيءَ قبلهما.

   ═══ والسؤالُ الثاني تابعٌ لا مستقلّ ═══

   لا معنى لـ«هل ترغب في التسجيل؟» عند من يملك تسجيلاتٍ أصلا. فيُخزَّن
   `wantsToRecordCourses` فارغا متى كان الأوّلُ «نعم» — لا يُحمل جوابٌ قديمٌ
   عن حالٍ تغيّرت. و`recordingAnswerPatch` أدناه تفرض هذا في موضعٍ واحدٍ
   تقرؤه الشاشةُ والخادمُ معا، فلا ينحرف أحدُهما عن الآخر. */

/** جوابا السؤال الأوّل — القيمةُ تُخزَّن منطقيّةً والاسمُ يُقرأ */
export const RECORDING_ANSWERS = [
  { value: 'yes', label: 'نعم — عندي تسجيلاتٌ جاهزة' },
  { value: 'no', label: 'لا — ليست عندي بعد' },
] as const

/** وجوابا السؤال الثاني — لا يُعرضان إلّا لمن قال «لا» */
export const WANTS_RECORDING_ANSWERS = [
  { value: 'yes', label: 'نعم — يهمّني أن أسجّلها معكم' },
  { value: 'no', label: 'لا — أفضّل اللقاءات المباشرة وحدَها' },
] as const

/** لماذا نسأل — يُقرأ في النموذج، ويُكتب مرّةً واحدةً فلا يتفرّق نصُّه */
export const RECORDINGS_WHY =
  'أفضلُ ما يتعلّم به الطالبُ عندنا تسجيلاتٌ مُعدّةٌ مسبقا يتبعها لقاءاتٌ مباشرةٌ معك — '
  + 'فنسأل لنعرف من أين نبدأ، لا لنشترط.'

/** «نعم»/«لا» إلى منطقيّ — وما سواهما لا جواب (`undefined` لا `false`) */
export function recordingAnswerToBool(v: string | null | undefined): boolean | undefined {
  return v === 'yes' ? true : v === 'no' ? false : undefined
}

/** والمنطقيُّ إلى «نعم»/«لا» — و`null` يعود فراغا لا «لا» */
export function boolToRecordingAnswer(v: boolean | null | undefined): string {
  return v === true ? 'yes' : v === false ? 'no' : ''
}

/** ما يُخزَّن من الجوابَين معا — الرغبةُ تسقط متى كانت التسجيلاتُ موجودة.

    موضعٌ واحدٌ تقرؤه الشاشةُ (لتُخفي السؤالَ الثاني) والخادمُ (لئلّا يكتب
    رغبةً عن حالٍ لا تُسأل فيها). ولو كُتب الشرطُ مرّتين لبقي في القاعدة
    «يرغب في التسجيل» لمن عاد وقال إنّ عنده تسجيلاتٍ — وهو تناقضٌ صامت. */
export function recordingAnswerPatch(has: string | null | undefined, wants: string | null | undefined): {
  hasCourseRecordings: boolean | undefined
  wantsToRecordCourses: boolean | undefined
} {
  const hasBool = recordingAnswerToBool(has)
  return {
    hasCourseRecordings: hasBool,
    /* لا تُسأل الرغبةُ إلّا عند «لا» صريحة — فلا عند «نعم» ولا قبل الجواب */
    wantsToRecordCourses: hasBool === false ? recordingAnswerToBool(wants) : undefined,
  }
}

/** كيف يُقرأ جوابُ التسجيلات عند من يراجع — نصٌّ واحدٌ لملفّ اللجنة ولشاشة
    الإدارة معا.

    ═══ ولماذا دالّةٌ لا سطرٌ في كلّ شاشة ═══

    الشرطُ ثلاثيٌّ (`true` · `false` · لا جواب)، وثلاثيٌّ يُكتب مرّتين يصير
    ثنائيّا في إحداهما بعد أوّل تعديل: يكتب كاتبٌ `!has` فيقرأ المراجعُ في
    شاشةٍ «لا تسجيلات عنده» وفي أخرى «لم يُسأل» عن الرجل نفسِه. وهذا يقع في
    هذا المستودَع — ولذلك يُفحص النصُّ هنا سلوكا، وتُفحص الشاشتان على أنّهما
    تناديان لا تكرّران. */
export function recordingsSummaryAr(
  has: boolean | null | undefined,
  wants: boolean | null | undefined,
): string {
  if (has === true) return 'نعم — عنده تسجيلاتٌ جاهزةٌ لدوراته'
  /* وما ليس `false` صريحةً فهو صمت — لا نفي. وهي حالُ كلّ طلبٍ سبق السؤال */
  if (has !== false) return 'لم يُسأل — طلبٌ سبق هذا السؤال'
  const tail = wants === true ? ' — ويرغب في تسجيلها معنا'
    : wants === false ? ' — ولا يرغب، يفضّل اللقاءات المباشرة'
      : ''
  return `لا تسجيلاتٍ عنده بعد${tail}`
}

/** الحالاتُ التي ما زال فيها الطلبُ حيّا ويجوز لصاحبه سحبُه */
export const WITHDRAWABLE_STATUSES = [
  'draft', 'email_verification_pending', 'submitted', 'under_review', 'information_requested',
  'shortlisted', 'interview_scheduled', 'demo_requested', 'academic_review',
  'conditionally_approved', 'contract_pending', 'waitlisted',
] as const

/** الحالاتُ التي يُعرض فيها حجزُ المقابلة — والحدُّ من طرفَيه مقصود.

    لا قبلها: `draft` و`email_verification_pending` طلبٌ لم يصل بعد، فيُكمله
    أوّلا ولا يحجز موعدا لما لا يُقرأ.

    ولا بعدها: `interview_scheduled` موعدُه محجوزٌ فعلا — وزرُّ «احجز» فوقه
    يدعوه إلى حجزٍ ثانٍ. وما بعده (ديمو · مراجعة · عقد · نشط) مرّ المقابلةَ،
    و`rejected` و`withdrawn` انتهيا. */
export const BOOKABLE_STATUSES: readonly string[] = [
  'submitted', 'under_review', 'information_requested', 'shortlisted',
]

/* ═══ من يُذكَّر بحجز موعده — مِحَكٌّ واحدٌ للشاشة وللخادم ═══

   الشاشةُ تعرض الزرَّ، والخادمُ يردّ الطلبَ. ولو كُتب الشرطُ مرّتين لانحرف
   أحدُهما يوما، فيُعرض زرٌّ يُردّ ٤٠٩ — أو يُخفى زرٌّ كان يعمل. وهذا يقع في
   هذا المستودَع: `PHASE2_OPEN_STATUSES` مكتوبٌ فوقُ أنّه نظيرٌ لهذا بعينه.

   وطرفاه مقصودان: الحالةُ تقبل الحجزَ (لا مسوّدةٌ ولا قرارٌ وقع)، ولا موعدَ
   قائمٌ له — والملغى لا يُحسب موعدا، فمن ألغى أحوجُ الناس إلى التذكير. */
export function canRemindToBook(app: { status: string; liveInterviews: number }): boolean {
  return BOOKABLE_STATUSES.includes(app.status) && app.liveInterviews === 0
}

/** الحالاتُ التي يجوز لصاحب الطلب أن يعدّله فيها.

    قرارُ صاحب المنصّة (٨ سبتمبر ٢٠٢٦): «اسمح له بتعديل طلب الانضمام طيلة
    فترة انتظاره حتى تتم الموافقة». وهي مطابقةٌ لـ`PHASE2_OPEN_STATUSES` في
    الخادم — ولو افترقتا لَأظهرت الشاشةُ زرّا يردّه الخادمُ ٤٠٩. */
export const EDITABLE_STATUSES: readonly string[] = [
  'draft', 'submitted', 'under_review', 'waitlisted',
  'information_requested', 'shortlisted', 'interview_scheduled', 'demo_requested', 'academic_review',
]

/* سنواتُ الخبرة كما تُقرأ — يقرؤها ملفُّ المتقدّم وشريطُ الحقائق معا.

   ═══ ونقصٌ كُشف بالنظر في الصفحة لا بالشيفرة (١٣ سبتمبر ٢٠٢٦) ═══

   كانت قيمُ `TRAINING_YEARS` الثلاثُ غائبةً عن هذا المعجم، و`yearsLabel`
   تردّ المفتاحَ كما هو عند العجز. فكان صفُّ «خبرة التدريب تحديدا» يُظهر
   `formal_teaching` **نصّا لاتينيّا** لمن يقرأ الملفّ.

   ومرّ طويلا لأنّه لا يُحمّر شيئا: الصفحةُ تعمل، والقيمةُ تُعرض، وإنّما
   تُعرض بلغةِ القاعدة. وإنّما ظهر حين فُتحت الصفحةُ المشتركةُ في متصفّحٍ
   ونُظر فيها — وهناك يقرؤها من هو خارج المنصّة أصلا.

   ويحرسه الآن فحصٌ بنيويّ: كلُّ قيمةٍ في `TRAINING_YEARS` و`DOMAIN_YEARS`
   تُترجَم، فلا تُضاف قيمةٌ غدا فتتسرّب مفتاحا. */
const YEARS_AR: Record<string, string> = {
  '1-3': '١–٣ سنوات', '4-7': '٤–٧ سنوات', '8-12': '٨–١٢ سنة', '12+': 'أكثر من ١٢ سنة',
  none: 'لا خبرة تدريب', under_1: 'أقلّ من سنة', '1_3': '١–٣ سنوات', '3_5': '٣–٥ سنوات',
  '5_10': '٥–١٠ سنوات', '10_plus': 'أكثر من ١٠ سنوات',
  informal: 'تدريبٌ غير رسميّ (زملاء وفريق)', workshops: 'ورشٌ ودوراتٌ قصيرة',
  formal_teaching: 'تدريبٌ منهجيٌّ معتاد (دورات وشعب)',
}

export function yearsLabel(v?: string | null): string {
  return v ? YEARS_AR[v] ?? v : '—'
}
