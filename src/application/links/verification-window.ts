/* مهلةُ روابط توثيق البريد — رقمٌ واحدٌ وجملةٌ واحدة.

   ═══ القرار ═══

   قرارُ صاحب المنصّة (١٩ سبتمبر ٢٠٢٦): «الرابط صالح لمدّة ٧ أيّام غير
   احترافيّة. يجب أن يكون لـ٢٤ ساعة بحدّ أقصى». وكان المداها اثنين لا واحدا:
   سبعةُ أيّامٍ لرابط طلب الانضمام، وثمانٍ وأربعون ساعةً لرابط توثيق الحساب.
   فصارا رقما واحدا هنا، وهو **سقفٌ** لا اقتراح — يحرسه
   `src/tests/verification-link-window.test.ts`.

   ═══ ولماذا الجملةُ مع الرقم ═══

   المدّةُ تُقال للقارئ في ثلاثة مواضع: بريدُ تأكيد طلب الانضمام، وبريدُ
   توثيق الحساب، وشاشةُ الرابط المنتهي. وكانت مكتوبةً بيدٍ في كلٍّ منها —
   فتغيّر الرقمُ في الخدمة وتبقى الجملةُ تَعِد بسبعة أيّام، ولا يحمرّ شيء.
   وهذا وقع فعلا في هذا المستودَع مع مدّةٍ أخرى (مدّةُ إعادة التقديم في رسالة
   الاعتذار). فالرقمُ والجملةُ يخرجان من هنا معا.

   ولا تشمل هذه المهلةُ **دعوةَ إنشاء الحساب** (`sendStaffInviteEmail`): تلك
   ليست توثيقَ عنوانٍ بين يدَي صاحبه، بل بابُ حسابٍ يُفتح لمن لم يدخل بعد —
   ومداها قرارٌ مستقلٌّ لم يُنقض. */

/** سقفُ صلاحيّة رابط التوثيق بالساعات — لا يُرفع فوق ٢٤ */
export const VERIFY_LINK_TTL_HOURS = 24

/** المهلةُ بالميلّي ثانية — تُحسب منها لحظةُ الانتهاء في الخادم */
export const VERIFY_LINK_TTL_MS = VERIFY_LINK_TTL_HOURS * 3_600_000

/** المدّةُ كما تُقرأ: «الرابط صالحٌ ‹أربعا وعشرين ساعة›» — نصبا بعد «صالح» */
export const VERIFY_LINK_WINDOW_AR = 'أربعا وعشرين ساعة'
