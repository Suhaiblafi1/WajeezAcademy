-- دورةٌ قائمةٌ بنفسها تُرشَّح بمهاراتها — ومجالُها يُعلَن.
--
-- إضافيّةٌ كلُّها: عمودٌ واحدٌ جديدٌ بقيمةٍ افتراضيّةٍ فارغة. ولا صفَّ قائمٌ
-- يتغيّر سلوكُه، ولا جدولٌ حيٌّ يُقفَل.
--
-- و`recommendableDirectly` قائمٌ في الجدول منذ أوّل المخطّط ولم يُقرأ قطّ،
-- فلا يُضاف هنا — وإنّما صار له معنى، وشرحُه في المخطّط عند موضعه.
--
-- ⚠️ ولمَ المجالُ عمودٌ لا يُشتقّ: المهاراتُ لا تحمل مجالا في هذا المخطّط،
-- والمجالُ يُخرَط من المسار أو الهدف أو الوظيفة. فدورةٌ بلا مسارٍ لا مجالَ
-- لها بالبناء — ومن دونه لا يصلها هدفٌ ولا احتياج، فتُحسب ملاءمتُها صفرا
-- مهما كانت مهاراتُها. والعمودُ الفارغُ يعني «لا تُرشَّح وحدَها» لا «كلُّ
-- المجالات»، ويحرسه `src/tests/diagnostic/standalone-course.test.ts`.

ALTER TABLE "Course" ADD COLUMN "diagnosticDomains" TEXT[] NOT NULL DEFAULT ARRAY[]::TEXT[];

-- وجمهورُها معه: `assessEntityEligibility` تُسقط كلَّ كيانٍ لم يُعلَن جمهورُه
-- — «عقوبةُ الصمت أن تخرج من المنافسة لا أن تدخل كلَّ منافسة». فلو تُرك
-- الجمهورُ فارغا لدخلت الدورةُ الفضاءَ ولم تنافس مرّةً واحدة.
ALTER TABLE "Course" ADD COLUMN "diagnosticStages" TEXT[] NOT NULL DEFAULT ARRAY[]::TEXT[];
