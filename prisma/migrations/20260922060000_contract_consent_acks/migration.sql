-- الجملُ التي أقرّ بها الموقِّعُ تُحفَظ معه — لا رقمُ إصدارها وحدَه.
--
-- رأسُ `contract-body.ts` يقول منذ كُتب إنّ «نصوصَها تُحفَظ مع العقد لا يُشار
-- إليها، فصياغتُها تتغيّر والموقَّعُ عليه لا يتغيّر» — ولم يكن يُحفَظ منها
-- شيء: `consentTextAr` جملةُ التوقيع وحدَها، و`consentVersion` في الأثر.
--
-- ولم يظهر ما دام الإصدارُ واحدا. وقد صار اثنين في ٢٢ سبتمبر ٢٠٢٦ (أُضيف
-- الإقرارُ السادسُ في البند 4-10)، فمن سُئل «بأيّ الجمل أقرّ؟» عن عقدٍ وُقّع
-- أمس لا يُجاب إلّا بالنبش في تاريخ Git.
--
-- إضافيّةٌ كلُّها: عمودٌ واحدٌ يقبل الفراغ. والعقودُ الموقَّعةُ قبله تبقى
-- فارغةً بحقّ — لم يُحفَظ لها شيءٌ يومَها، ولا يُخترع لها الآن ما لم يُسجَّل.
-- ويُقرأ فراغُها بـ`consentVersion` في أثرِ توقيعها كما كان.

ALTER TABLE "TrainerContract" ADD COLUMN "consentAcksAr" JSONB;
