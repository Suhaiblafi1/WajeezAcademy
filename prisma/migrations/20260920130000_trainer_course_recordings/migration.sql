-- تسجيلاتُ دورات المتقدّم — سؤالٌ وجوابُه يفتح سؤالا (٢٠ سبتمبر ٢٠٢٦)
--
-- العمودان منطقيّان **يقبلان العدم**، وهو المقصود: `NULL` تعني «لم يُجب»،
-- وهي غيرُ `false` التي تعني «قال لا». والسؤالُ الثاني لا يُعرض إلّا على من
-- قال لا — فلو كان العمودُ `NOT NULL DEFAULT false` لَصار كلُّ طلبٍ سابقٍ
-- كأنّه أجاب «لا»، ولانفتح عليه سؤالٌ لم يُسأله أحد.
--
-- ولهذا لا قيمةَ افتراضيّةَ هنا: الطلباتُ التي سبقت هذا الترحيل تبقى بلا
-- جواب، وتُقرأ في الملفّ «لم يُسأل» لا «لا».
ALTER TABLE "TrainerApplication" ADD COLUMN "hasCourseRecordings" BOOLEAN;
ALTER TABLE "TrainerApplication" ADD COLUMN "wantsToRecordCourses" BOOLEAN;
