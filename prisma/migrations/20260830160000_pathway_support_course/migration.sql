-- الدورة المساندة على رابط المسار: سببها نصّ يُعرض للمتعلّم، فيُخزَّن معها.
-- المساندة تُميَّز بـ kind = 'support'، وهي **خارج** دورات المسار الأساسية:
-- بناء اللقطة يقصر course_ids على ما ليس مساندة، لأنّ تلك القائمة وحدها
-- يقرؤها pathwaySkills فتُحسب منها فجوة المهارات في التشخيص.
ALTER TABLE "PathwayCourse" ADD COLUMN IF NOT EXISTS "reasonAr" TEXT;
