-- من أين طُلبت المعلوماتُ الإضافيّة، ليعود إليه المتقدّمُ لا إلى أوّل الطابور.
-- بلا قيدِ CHECK: قيمتُه لا تأتي إلّا من `status` نفسِه لحظةَ الطلب، فهي
-- مقيَّدةٌ بقيده. ويُمحى بالرجوع فلا يبقى وعدٌ منقضٍ.
ALTER TABLE "TrainerApplication" ADD COLUMN "infoRequestedFrom" TEXT;
