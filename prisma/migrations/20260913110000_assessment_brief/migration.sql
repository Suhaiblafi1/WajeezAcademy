-- تعليماتُ التكليف — ما يفعله المتعلّم بالضبط.
--
-- كان التكليفُ يصل المتعلّمَ بعنوانٍ ولا شيءَ غيرِه: «تكليف: تحرير خبر»
-- ثمّ صندوقُ تسليم. فما المطلوبُ تحريرُه؟ وفي كم كلمة؟ وبأيّ معيارٍ
-- يُقاس؟ كلُّ ذلك كان يُقال في اللقاء المباشر ويُنسى بعده، أو يُكتب في
-- عنوانِ التكليف نفسِه فيطول العنوانُ حتّى يخرج من بطاقته.
--
-- ولم يكن `AssessmentItem` موضعَه: تلك أسئلةُ الاختبار، يتحقّق منها
-- `submitAttempt` بمطابقة الأجوبة بمعرّفاتها — فوضعُ التعليمات فيها
-- يجعل كلَّ واجبٍ اختبارا من سؤالٍ واحد.
--
-- والعمودُ NULL ابتداءً: التكاليفُ القائمةُ أُنشئت بلا تعليمات، ولا
-- يُختلق لها نصّ.
ALTER TABLE "CohortAssessment" ADD COLUMN "briefAr" TEXT;
