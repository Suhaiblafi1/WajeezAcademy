-- الرمزُ يُخزَّن نصّا صريحا — ليُنسَخ الرابطُ نفسُه لا رابطٌ جديد.
--
-- قرارُ صاحب المنصّة (١٤ سبتمبر ٢٠٢٦)، بعد أن عُرض عليه البديلان وأُعيد
-- عليه الثمنُ مرّتَين: «تخزين»، ثمّ «عاريا — الرابط ليس سرّيّا لهذه الدرجة».
--
-- ── والثمنُ يُكتب هنا لأنّه لا يُرى في الشيفرة ──
--
-- كان التصميمُ يحفظ `sha256(token)` وحدَه، فلا نسخةَ من الرابط في أيّ مكان:
-- لحظةُ الإنشاء هي الوحيدةُ التي يُعرف فيها. وبهذا العمود يصير لكلّ رابطٍ
-- حيٍّ نسخةٌ في القاعدة.
--
-- ومعنى ذلك عمليّا: **كلُّ مَقلَبٍ ليليٍّ يصير دفترَ روابطَ حيّة**. والمَقلَبُ
-- يستقرّ على قرص الخادم ويُحمل في نسخ Hetzner السحابيّة — ملفٌّ يُنسخ ويُنقل
-- ويُسترجَع في مواضعَ ليست الخادمَ نفسَه.
--
-- ومن ملك رابطا فتح — بلا حساب — ملفَّ المتقدّم كاملا: بياناتِه، وروابطَ
-- موقَّعةً تُنزّل سيرتَه وشهاداتِه، ومُعرِّفيه بأسمائهم وملاحظاتهم، وتوقّعاتِه
-- الماليّة، وكلامَ المراجعين فيه. ولهذا تقول قاعدةُ المستودَع: «ولا يصل
-- الرابطُ المتقدّمَ أبدا».
--
-- ── وما يخفّف، وما لا يخفّف ──
--
-- يبقى: المهلةُ (`expiresAt`)، والإبطالُ (`revokedAt`)، وأنّ الرمزَ عشوائيٌّ
-- بأربعةٍ وعشرين بايتا. ولا يبقى: أنّ تسريبَ نسخةٍ احتياطيّةٍ كان بلا أثر.
--
-- والعمودُ اختياريٌّ بقصد: الروابطُ القائمةُ قبل هذه الهجرة لا رمزَ لها
-- ولا يُستخرج من هاشها — فتلك يُجدَّد رابطُها ولا يُنسَخ.
ALTER TABLE "TrainerDossierLink"
  ADD COLUMN "tokenClear" TEXT;
