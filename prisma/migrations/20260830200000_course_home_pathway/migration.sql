-- المسار الأمّ للدورة وترتيبها فيه — صريحان على الدورة لا مستنتَجان من روابطها.
--
-- كان يُستنتج من أوّل رابط مسار، وصار ذلك خطأً حين صارت للدورة روابط مساندة
-- في مسارات أخرى: يلتقط الاستنتاج أحدها اعتباطا فتُنسب الدورة إلى مسارٍ
-- تسانده. والاستنتاج يعجز أصلا عن دورةٍ لا رابط أساسيّ لها البتّة —
-- «دورة الإنجليزية للأعمال» مساندةٌ في أربعة مسارات وليست في مقرّرات أيٍّ
-- منها، فلا شيء في القاعدة يقول أين بيتها.
ALTER TABLE "Course" ADD COLUMN IF NOT EXISTS "homePathwayId" TEXT;
ALTER TABLE "Course" ADD COLUMN IF NOT EXISTS "homeSequence" INTEGER;
