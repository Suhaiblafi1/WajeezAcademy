# البنية التحتيّة والنشر — أكاديمي وجيز

> # ⚠️ تصويبٌ جوهريّ — ٦ سبتمبر ٢٠٢٦
>
> **كانت هذه الوثيقةُ تقول إنّ الاستضافة Cloudways. وهو خطأ.** والخطأُ لم يبقَ
> على الورق: بُني عليه `edc7576` (#25) فحذف `deploy/` — **وهو إعدادُ الخادم
> الذي يخدم النطاق فعلا**.
>
> **والقياسُ هو الحَكَم:** `www.wajeezacademy.com` يُحلّ إلى **`5.9.82.49`**،
> وهو في نطاق Hetzner؛ وCloudways لا تقدّم Hetzner. وأكّده من يملك الخادم:
> حاويات Docker وCaddy، ولا وجودَ لـ`/etc/supervisor/conf.d` عنده. والتسجيلاتُ
> الأربعةُ وخطّافُ Stripe كلُّها تصل إلى ذلك الخادم.
>
> فأُعيد `deploy/` في `#29`، وصُحّحت هذه الوثيقةُ لتصف ما يعمل.
>
> **والدرسُ مكتوبٌ لئلّا يُعاد:** «الوثيقةُ تقول» ليست قياسا. قبل حذف أيّ
> إعدادِ استضافةٍ يُسأل الـDNS: `dig +short www.wajeezacademy.com`.
>
> ---
>
> **وهذه الوثيقةُ هي المرجعُ للواقع الحاليّ.** ما خالفها في هذا المستودَع سجلٌّ
> تاريخيٌّ لا تعليماتٌ تُتّبع — ومنه خطّةُ النقل في `audit-2026-09/04`.
>
> وحُذف مسارُ Vercel القديم (`vercel.json`، `api/index.js`، وما يبنيهما)
> نهائيّا، وذلك حذفٌ صحيح: النطاقُ لم يكن عليه. راجع تاريخَ Git إن احتجتَه.
>
> **والنشرُ كان يدويّا:** `bash deploy/deploy.sh` على الخادم. ولم يوجد نشرٌ
> آليٌّ ناجحٌ في تاريخ المستودَع قطّ — تسعُ تشغيلاتٍ لسير عمل `Deploy` منذ ٣٠ آب،
> منها سبعُ فشلٍ وتخطّيان وصفرُ نجاح، وسببُ آخرِها أنّ أسرارَ SSH الخمسةَ
> كلَّها فارغة. فحُذف ما كان يصف ذلك النشرَ في #30.
>
> **وبُني بعده نشرٌ آليٌّ على الخادم الحقيقيّ — بالسحب لا بالدفع:** الخادمُ
> يسأل عن `main` بنفسه ([`scripts/deploy-watch.sh`](../scripts/deploy-watch.sh))
> ويشغّل [`deploy/deploy.sh`](../deploy/deploy.sh). **ولا يخرج منه مفتاحٌ ولا
> سرّ** — وهو شرطُ صاحب المنصّة. التفصيلُ في §٢-ب.


---

> **ومختصرُ ما يحتاج يدَ من يملك الخادم في [`OPS_AR.md`](OPS_AR.md)** — وظيفةُ
> Supervisor، ومتغيّراتُ البيئة، وأوّلُ مديرِ نظام. أرسله إليه كما هو.

## ١) الواقع اليوم

| | |
|---|---|
| **النطاق** | `https://www.wajeezacademy.com` → **`5.9.82.49`** (Hetzner) |
| **الاستضافة** | **خادمٌ ذاتيٌّ على Hetzner** — حاويات Docker: Postgres · خادم وجيز · Caddy. الإعدادُ في [`deploy/`](../deploy/) |
| **الـAPI** | خادم Fastify في حاوية، خلف Caddy — لا ينشر منفذا إلى المضيف |
| **الترويسات** | ⚠️ **[`deploy/Caddyfile`](../deploy/Caddyfile)** — CSP وHSTS و`nosniff`. **و`public/.htaccess` لا يُقرأ هنا**: هو ملفُّ Apache وCaddy لا يعرفه |
| **قاعدة البيانات** | PostgreSQL في حاوية `db` — لا تنشر منفذا، ويصلها الخادمُ عبر شبكة Docker |
| **النشر** | `bash deploy/deploy.sh` على الخادم |

> ### ⚠️ وCloudways: لا يُعرف أنّ لها وجودا
>
> **ولم يُعثر على أثرٍ واحدٍ يسندها.** والقياسُ ثلاثةٌ يشدّ بعضُه بعضا:
>
> | | |
> |---|---|
> | مصدرُ الادّعاء | `da486da` (٥ سبتمبر): «**قال صاحبُ المنصّة** إنّ الموقع على خادم Cloudways». جملةٌ واحدةٌ لم تُقَس، ورثتها كلُّ وثيقةٍ بعدها |
> | عنوانُ خادم Cloudways في المستودَع | **لم يُضبط قطّ** — `SSH_HOST` فارغٌ في كلّ تشغيلة |
> | نشراتٌ آليّةٌ نجحت | **صفر** من تسع |
>
> فحُذف في #30 ما كان مصوَّبا عليها: `scripts/deploy-cloudways.sh` و
> `scripts/deploy-watch.sh` وحارسُهما. **وبقي**
> [`SERVER_ENV_AR.md`](SERVER_ENV_AR.md) و[`OPS_AR.md`](OPS_AR.md) موسومَين
> بتنبيهٍ في صدرهما: «لماذا» فيهما صحيحٌ على أيّ مضيف، و«كيف» مصوَّبٌ على
> Supervisor فلا يُنفَّذ هنا.

### وما حُذف من المستودَع — كان خاصّا باستضافةٍ لم تعد قائمة

- **مسارُ Vercel كاملا**: `vercel.json`، والحزمةُ المبنيّةُ لدالّته السحابيّة
  (`api/index.js`)، وما يبنيها (`scripts/bundle-api.mjs`،
  `scripts/vercel-build.sh`، `server/http/vercel-handler.ts`)، وبوّابةُ بصمات
  الـCSP التي كانت تكتب فيه (`scripts/audit-csp.ts`). ترويساتُ الأمان —
  سياسةُ المحتوى، واستثناءُ عامل الخدمة — انتقلت إلى `public/.htaccess`، وهو
  ما يُقرأ فعلا على Cloudways.
- ~~**تصميمُ الخادم الذاتيّ**: `deploy/` — «لم يُستعمل قطّ في الإنتاج»~~
  **← هذا خطأ، وقد أُعيد.** كان ذلك الوصفُ ادّعاءً لا قياسا، وهو ما حذف
  إعدادَ الخادم الحيّ. انظر التصويبَ في صدر الوثيقة و[`deploy/README.md`](../deploy/README.md).

> ### ⚠️ وترويساتُ الأمان: أين تُطبَّق فعلا
>
> نُقلت في #25 من `vercel.json` إلى `public/.htaccess` بوصفه «ما يُقرأ فعلا على
> Cloudways». **وعلى خادم الإنتاج لا يُقرأ**: `.htaccess` ملفُّ Apache، والخادمُ
> يقدّم الواجهةَ بـCaddy — وCaddy لا يعرفه بتاتا.
>
> فمصدرُها هناك [`deploy/Caddyfile`](../deploy/Caddyfile) وحدَه. ولو حُدِّث
> الخادمُ إلى `main` بلا `deploy/` **لسقطت CSP وHSTS و`nosniff` صامتةً** — بلا
> خطأ، وبلا اختبارٍ يحمرّ: `src/tests/deploy/htaccess-spa-rewrite.test.ts`
> يقرأ **نصَّ الملفّ** ولا يتحقّق من أنّ خادمَ ويبٍ يقرؤه. وهو حارسٌ صحيحٌ في
> نفسه، لكنّه لا يحرس هذا.

### ١-ب) ⚠️ إغلاقُ حساب Vercel — ما لا يفعله المستودَع ولا يفعله أحدٌ غيرُك

حذفُ الملفّات من المستودَع **لا يُطفئ شيئا عند المزوّد**. المشروعُ على Vercel
يبقى قائما، ونطاقُه `‎*.vercel.app` يبقى قابلا للفتح، وربطُه بـGitHub يبقى
يبني عند كلّ دفعة. وهذه قائمةُ ما يُفعل في لوحة Vercel نفسها — **بهذا الترتيب**،
ولا يُبدأ بالحذف:

| # | الخطوة | لماذا هنا لا هناك |
|---|---|---|
| **١** | **انسخ متغيّراتِ البيئة أوّلا** — Project → Settings → Environment Variables | فيها قيمٌ قد لا تكون مكتوبةً في مكانٍ آخر: `DATABASE_URL`، `STORAGE_SECRET`، `RESEND_API_KEY`، `PAYMENT_*`. **وحذفُ المشروع يحذفها بلا رجعة** |
| **٢** | **ثمّ دوّرها كلَّها** — كلمةُ سرّ القاعدة، ومفاتيحُ Resend وStripe، و`STORAGE_SECRET` | عاشت في نظامٍ لم يعد تحت يدك. و⚠️ تدويرُ `STORAGE_SECRET` **يُبطل كلّ رابطٍ موقَّعٍ سارٍ** لوثائق المتقدّمين — تُعاد صياغتُها عند الطلب، فليست خسارةَ بيانات |
| **٣** | **افصل ربطَ GitHub** — Project → Settings → Git → Disconnect | ما دام موصولا، كلُّ دفعةٍ إلى `main` تُطلق بناءً على Vercel: دقائقُ تُحتسب، ونشرةٌ قد تنجح فتُحيي الموقعَ القديم، وتعليقاتٌ على طلبات السحب تُربك المراجع |
| **٤** | **افحص تخزينَ Vercel** — Storage (Blob · Postgres · KV) | إن كان فيه شيء **فهو يموت مع المشروع**. والمنصّةُ اليومَ لا تستعمله: الوثائقُ في عمود `Bytes` في القاعدة (`server/services/storage.service.ts`)، والقاعدةُ على `DATABASE_URL` — فالمتوقَّعُ أن يكون فارغا، **والمتوقَّعُ يُتحقَّق منه لا يُفترض** |
| **٥** | **انزع النطاقَ من المشروع** — Settings → Domains → Remove | قبل الحذف لا بعده. والنطاقُ اليومَ يشير إلى الخادم الجديد (تحقّق: `dig +short www.wajeezacademy.com`)، لكنّ بقاءَه مسجَّلا عند Vercel يُبقي شهادةً وتوجيها قد يعودان للعمل |
| **٦** | **ثمّ احذف المشروع** — Settings → Delete Project | آخرَ خطوة. وبها يموت `‎https://wajeez-academy.vercel.app` |
| **٧** | **ألغِ الاشتراك** — Account → Billing | الحذفُ لا يُلغي الخطّة المدفوعة |

#### وثلاثةُ عناوينَ خارج Vercel تُراجَع بعدها

- **خطّافُ Stripe** — لوحةُ Stripe → Developers → Webhooks. إن كان مسجَّلا على
  `‎*.vercel.app` فهو يُرسل إلى عنوانٍ ميّت: **مالٌ يُقبض والتسجيلُ لا يُسوّى.**
  الصحيح: `https://www.wajeezacademy.com/api/webhooks/payments/stripe` (§٥).
- **نطاقُ Resend** — تحقّقُ الإرسال (SPF/DKIM) مربوطٌ بنطاقك لا بالمضيف، فلا
  يتأثّر. لكن راجع `APP_URL` في بيئة الخادم: **منه تُبنى روابطُ الرسائل** (§٣).
- **`‎*.vercel.app` في الفهرسة** — إن كان قد فُهرس، فحذفُ المشروع يجعله ٤٠٤
  ويسقط من قوقل وحدَه خلال أسابيع. وهذا أحدُ وجهَي شكوى «الموقعُ القديمُ
  يتصدّر» (§٤ للوجه الآخر).

#### وما في المستودَع اليومَ ممّا كان لـVercel

| الموضع | الحال |
|---|---|
| `scripts/vercel-build.sh` · `vercel.json` · `api/index.js` · `server/http/vercel-handler.ts` | **محذوفةٌ نهائيّا** |
| `VERCEL_ENV` في حارس الديمو | **استُبدل بـ`APP_ENV`** — §٣، واضبطه وإلّا فالحارسُ أعمى |
| `VERCEL_PROJECT_PRODUCTION_URL` في `vite.config.ts` | **حُذف** — المصدرُ `VITE_SITE_ORIGIN` ثمّ النطاقُ الحيّ |
| نطاقُ `‎*.vercel.app` في دعوات التقويم | **حُذف** — المصدرُ `publicSiteUrl()` أي `APP_URL` |
| `VERCEL_*` في `server/build-stamp.ts` | **باقيةٌ عمدا.** ليست ارتباطا بمضيف: حلقةٌ في سلسلةِ أسماءٍ يتصدّرها `GIT_COMMIT_SHA` وفيها `SOURCE_VERSION` (Heroku) — والمصدرُ الفعليُّ ملفُّ الختم المكتوبُ وقتَ البناء |
| ذِكرُ Vercel في التعليقات | **باقٍ حيث يروي تاريخا**: «كان كذا فصار كذا» يمنع إعادةَ عطبٍ أُصلح. وصُحّح كلُّ ما كان يصف Vercel بصيغة الحاضر أو يحيل إلى ملفٍّ محذوف |

---

## ٢) ⚠️ ما يجب أن يفعله سكربتُ النشر — وأخطرُ ما في هذه الوثيقة

**`npm run build` وحدَه لا يكفي.** هو `tsc -b && vite build` — يبني الواجهةَ فقط.

وكانت هذه الخطواتُ على Vercel في سكربتٍ **أربعةٌ منها مشروطةٌ بمتغيّرٍ اسمُه
`VERCEL_ENV` — وهو غيرُ موجودٍ على Cloudways**. فحُذف السكربتُ مع سطح Vercel
كلِّه، ويبقى الواجبُ أن تُكتب صراحةً في سكربت Cloudways — **وإلّا فهي لا تعمل**:

| الخطوة | إن لم تعمل |
|---|---|
| `npx prisma generate` | البناءُ يفشل أو الأنواعُ تكذب |
| **`npx prisma migrate deploy`** | **انحرافُ مخطَّط**: ترحيلاتٌ جديدةٌ لا تُنفَّذ، فمساراتُ الخادم تسقط بـ٥٠٠ على أعمدةٍ غيرِ موجودة |
| `npm run build` | الواجهةُ لا تتحدّث · **وختمُ البناء لا يُكتب**، فلا يُعرف أيَّ نسخةٍ يخدم الخادم |
| **`npm run catalog:import`** | **الكتالوجُ الذي يراه المتعلّمُ مجمَّد.** `‎/api/public/core-catalog` يقرأ **الجداولَ الحيّة**، والاستيرادُ هو ما يكتب فيها مسارات المستودَع ودوراتِه ومتونَه. فتصل شيفرةٌ جديدةٌ فوق **محتوًى قديمٍ لا يتغيّر** |
| **`npm run catalog:publish`** | **محرّكُ التشخيص مجمَّد** على لقطته القديمة — أسئلتُه وتوصياتُه |
| **`supervisorctl restart`** | **الـAPI كلُّه مجمَّد** — وهو ما وقع فعلا (انظر التحذير أدناه) |

> **وهذا احتمالٌ قائمٌ لم يُتحقَّق منه بعد** لشكوى «الموقعُ القديمُ يتصدّر»:
> الواجهةُ تتحدّث والمحتوى لا. **يُحسم بالخطوة الأولى في القسم ٤.**

### سكربتُ النشر — [`deploy/deploy.sh`](../deploy/deploy.sh)

```bash
bash deploy/deploy.sh              # نشرٌ من الفرع الحاليّ
SKIP_PULL=1 bash deploy/deploy.sh  # نشرُ ما في مجلّد العمل بلا جلب
```

وترتيبُه مقصود: **تُبنى الصورةُ الجديدة قبل لمس أيّ شيءٍ يعمل.** فإن أخفق
البناءُ أو الهجرةُ بقي الموقعُ القديم يخدم كأنّ شيئا لم يكن، ولا تُبدَّل
الحاويات إلّا بعد نجاحهما.

| الخطوة | إن لم تعمل |
|---|---|
| `docker compose build app` | لا صورةَ جديدة — ولا يُلمس القائم، وهذا مقصود |
| `prisma migrate deploy` | **انحرافُ مخطَّط**: مساراتُ الخادم تسقط بـ٥٠٠ على أعمدةٍ غيرِ موجودة |
| **`npm run catalog:import`** | **الكتالوجُ الذي يراه المتعلّمُ مجمَّد** — `‎/api/public/core-catalog` يقرأ الجداولَ الحيّة |
| **`npm run catalog:publish`** | **محرّكُ التشخيص مجمَّد** على لقطته القديمة |
| `docker compose up -d` | الصورةُ مبنيّةٌ ولا تخدم أحدا — **التبديلُ هو النشر** |

> **وحارسُها [`src/tests/deploy/production-deploy-steps.test.ts`](../src/tests/deploy/production-deploy-steps.test.ts)**
> يرفض سقوطَ أيٍّ منها، ويشترط بقاءَ البناء **قبل** التبديل. ومُثبَتٌ
> بالطفرة: نزعُ `catalog:import` يُحمّره، ونزعُ خطوة البناء يُحمّره.
>
> ⚠️ **وحدُّه صريح:** يُثبت أنّ الأمرَ مكتوبٌ في الملفّ، لا أنّه نجح على
> الخادم. وهو بعينه عطبُ `htaccess-spa-rewrite.test.ts`.

**والتبديلُ نفسُه يحلّ مشكلةَ «العمليّةُ لا تعلم بشيء»:** `docker compose up -d`
يستبدل الحاويةَ بصورةٍ جديدة، فلا يبقى في الذاكرة شيءٌ من الشيفرة القديمة.
وكان هذا عطبا حقيقيّا رُصد على هذه المنصّة حين كان النشرُ يُظنّ على مضيفٍ
بعمليّة Node على المضيف: الواجهةُ تعرض الجديدَ و`‎/api/version` يعرض القديم.

**والتحقّقُ بعد النشر مبنيٌّ في السكربت**: يسأل `‎/api/version` — محلّيّا ثمّ
على النطاق — ولا يُعلن نجاحا لأنّ الأوامرَ رجعت بصفر.

### ٢-ب) ما كان يُسمّى نشرا آليّا — ولم ينشر شيئا قطّ

**والقائمُ اليومَ في §٢-جـ.** هذا القسمُ تاريخٌ يفسّر لماذا حُذف ما حُذف.

كان في المستودَع سيرُ عملٍ (`‏.github/workflows/deploy.yml`) وسكربتٌ ينشر إلى
خادم Cloudways (`deploy-cloudways.sh`). **حُذفا في #30** — لا المراقبُ
`deploy-watch.sh`: ذاك يقرّر **متى** يُنشَر لا **كيف**، فلا شيءَ فيه خاصٌّ
بمضيف، وقد صُوّب على `deploy/deploy.sh` وبقي (§٢-جـ).

وسببُ الحذف بالقياس لا بالرأي:

| القياس | النتيجة |
|---|---|
| نشراتُ `Deploy` منذ ٣٠ آب | **٩ تشغيلات: ٧ فشلٌ · ٢ تخطٍّ · ٠ نجاح** |
| سببُ الفشل في آخرها | `SSH_HOST` و`SSH_USER` و`SSH_KEY` و`SSH_KNOWN_HOSTS` و`APP_PATH` — **كلُّها فارغة** |
| عنوانُ خادم Cloudways في المستودَع | **لم يُضبط مرّةً واحدة قطّ** |

أي أنّ ذلك المسارَ **لم ينشر شيئا مرّةً واحدة في تاريخ المستودَع**، وكان يصف
خادما لا وجودَ له.

> **وما ينقص فعلا** ليس سكربتا: **رصدٌ**. لا يصل إنذارٌ حين تفشل نشرةٌ ليلا
> (البند ٢٥ في `review-2026-09-06`) — والنشرُ الآليُّ في §٢-جـ لا يغيّر هذا:
> يكتب في سجلٍّ على الخادم، ولا يوقظ أحدا.

---

### ٢-جـ) والنشرُ الآليّ الذي بُني عليه — سحبٌ لا دفع

القسمُ أعلاه يختم بأنّ «بناءَ نشرٍ آليٍّ على الخادم الحقيقيّ عملٌ قائمٌ لم
يُنفَّذ — ويُبنى على `deploy/deploy.sh`». **وقد نُفِّذ.**

**وقيدُ صاحب المنصّة الذي شكّل الحلّ:** لا تخرج مفاتيحُ الخادم منه. فالنشرُ من
GitHub يقتضي أن يحمل GitHub مفتاحَ SSH، وذلك يعني الثقةَ — لا بـGitHub وحدَه —
بل بكلِّ من يملك صلاحيّةَ الكتابة في المستودَع. **فقُلب الاتّجاه: الخادمُ
يسأل، ولا يُسأل.**

[`scripts/deploy-watch.sh`](../scripts/deploy-watch.sh) يعمل على الخادم كلَّ بضع
دقائق: هل تحرّكت `origin/main` عن نسخته؟ فإن لم تتحرّك خرج صامتا، وإن تحرّكت
شغّل `deploy/deploy.sh`. **لا مفتاحَ خارج الخادم، ولا منفذَ مفتوحٌ لأحد، ولا
سرَّ يُشارَك.** والثمنُ تأخيرٌ يساوي دورةَ الجدولة.

> **والمراقبُ يقرّر متى يُنشَر لا كيف.** فلا يكرّر شيئا ممّا يفعله
> `deploy/deploy.sh` — لا نسخةً احتياطيّة، ولا بناءً قبل التبديل، ولا فحصا
> صحّيّا. ذلك كلُّه هناك، وأوثقُ.

#### التركيب — مرّةً واحدة

```bash
crontab -e
```

ثمّ سطرٌ واحد (بدّل المسارَ بمسار المستودَع على الخادم):

```
*/5 * * * * /bin/bash /مسار/المستودَع/scripts/deploy-watch.sh
```

وقبله فحصٌ لا يكتب شيئا: `bash scripts/deploy-watch.sh --check`.

#### والسجلّ

`~/wajeez-deploy.log` — ولا يُكتب فيه شيءٌ حين لا جديد، فسطرٌ كلَّ خمس دقائق
يُغرقه فلا يُقرأ حين يهمّ. والفشلُ يُكتب بعلامةٍ تُبحَث: `grep '✖'`.

> ⚠️ **وهذا لا يُغني عن الرصد.** لا يصل إنذارٌ حين تفشل نشرةٌ ليلا — السطرُ
> أعلاه هو ما يُقرأ، ويُقرأ بيدٍ. والرصدُ باقٍ بندا قائما.

#### وثلاثةُ حرّاسٍ فيه

| الحارس | العطبُ الذي يمنعه |
|---|---|
| قفلٌ بـ`mkdir` يُحرَّر بـ`trap` | دورةُ الجدولة أقصرُ من النشرة، فبلا قفلٍ تبدأ الثانيةُ فوق الأولى |
| رفضُ فرعٍ غيرِ `main` | خادمٌ تُرك على فرعٍ بعد فحصٍ يدويّ لا يُسحَب إليه `main` صامتا |
| خروجٌ صامتٌ حين لا جديد | سجلٌّ يمتلئ بلا شيءٍ لا يُقرأ حين يهمّ |

وحارسُه المصدريّ [`src/tests/deploy/deploy-watcher.test.ts`](../src/tests/deploy/deploy-watcher.test.ts)
— يحرس المراقبَ وحدَه ولا يمسّ متنَ `deploy/deploy.sh`، فلذاك حارسُه.



### أمرُ التشغيل — وما تضعه في وظيفة Supervisor

```
npm start
```

وهو `tsx server/index.ts` — خادمُ Fastify نفسُه. **ولا يحتاج بناءً**: الشيفرةُ
تُقرأ من القرص عند الإقلاع، فيكفي `git pull` ثمّ إعادةُ تشغيلٍ ليتحدّث الـAPI.

| المتغيّر | ماذا يضبط |
|---|---|
| `API_PORT` | المنفذ — الافتراض `7101` |
| `API_HOST` | العنوان — الافتراض `127.0.0.1` (المغلق). اضبطه `0.0.0.0` **فقط** إن كان الوسيطُ العكسيّ في حاويةٍ أخرى |
| `DATABASE_URL` | إلزاميّ. **وبدونه يحاول الخادمُ تشغيل قاعدةٍ مدمجةٍ للتطوير** |

> **ولماذا وُضع الأمرُ في المستودَع:** لم يكن موجودا، فكانت وظيفةُ Supervisor
> تشغّل أمرا مكتوبا باليد **خارج** المستودَع — لا يعرفه أحدٌ ولا يراجعه أحدٌ
> ولا يتحدّث حين تتغيّر الشيفرة. ويحرسه `server/tests/ops/start-command.test.ts`،
> ومعه حارسٌ أهمُّ منه: **ألّا تكون في مسار الإقلاع حزمةُ تطوير**. وقد أمسك
> واحدةً عند كتابته — `embedded-postgres` كانت تُستورَد استيرادا ثابتا، فتثبيتٌ
> إنتاجيٌّ رشيق (`npm ci --omit=dev`) كان يُسقط الخادمَ عند أوّل سطر.

---

## ٣) متغيّرات البيئة

### إلزاميّةٌ وقتَ **البناء** (تُقرأ في الحزمة، لا وقتَ التشغيل)

| المتغيّر | لماذا |
|---|---|
| `VITE_SITE_ORIGIN=https://www.wajeezacademy.com` | يُحقَن في `canonical` و`og:url` و`og:image` و`sitemap.xml` و`robots.txt`. **والبوتاتُ تقرأ هذه الوسومَ ولا تشغّل React** — فبدونه تُعلن كلُّ صفحةٍ نطاقا غيرَ نطاقك |

> **والاحتياطيُّ صار النطاقَ الحيّ** (`src/application/site/origin.ts` و`vite.config.ts`،
> ويحرس تطابقَهما `src/tests/site-origin.test.ts`). فالنسيانُ لم يعد كارثة —
> **لكنّ الضبطَ الصريحَ هو الصواب**، وهو الوحيدُ الذي يصمد لو تغيّر النطاق.

### إلزاميّةٌ وقتَ **التشغيل**

> **وللتنفيذ على الخادم مباشرةً:** [`SERVER_ENV_AR.md`](SERVER_ENV_AR.md) —
> خطواتُ ضبط `NODE_ENV` و`APP_ENV` في Supervisor، والتحقّقُ منها.

| المتغيّر | لماذا |
|---|---|
| `DATABASE_URL` | القاعدة |
| `APP_URL=https://www.wajeezacademy.com` | عليه تُبنى **روابطُ البريد** (تأكيدُ الحساب · استعادةُ كلمة السرّ · دعوةُ الموظّف) **والعودةُ بعد الدفع**. وبدونه يسقط إلى `http://localhost:7100` — رسالةٌ تصل برابطٍ لا يفتح عند أحد. والكودُ يرفض تفعيلَ Stripe بدونه بسببٍ مكتوب |
| `STORAGE_SECRET` | إن غاب، يُشتقُّ مفتاحُ توقيع الروابط من `DATABASE_URL` — **فتغييرُ عنوان القاعدة يُبطل كلَّ رابطٍ موقَّعٍ صامتا** |
| `PAYMENT_*` | القسم ٥ |
| `RESEND_API_KEY` | القسم ٦ — **وجودُه وحدَه يفعّل قناةَ البريد** |
| `NODE_ENV=production` | **أربعةُ حرّاسٍ معلَّقةٌ عليه**، وأخطرُها أنّ رمزَ استعادة كلمة السرّ يُردّ في جسم الاستجابة (`devToken`) بدونه — **استيلاءٌ على أيّ حسابٍ يُعرَف بريدُه**. ومعه: كعكةُ الجلسة بلا `secure`، وتوثيقُ Swagger مفتوحٌ للعموم، وتحذيرُ `STORAGE_SECRET` لا يظهر. ⚠️ **في بيئة التشغيل وحدَها** — انظر أدناه |
| `APP_ENV=production` | **يُعلن أنّ هذا نشرٌ إنتاجيّ.** يقرؤه حارسُ وضع الديمو (`server/http/routes/demo.routes.ts`) و`‎/api/version`. **وبدونه يظنّ الخادمُ نفسَه غيرَ إنتاجيّ**، فلو ضُبط `DEMO_MODE=true` يوما فُتح `‎/api/demo/switch-role` على الإنترنت — وقد وقع ذلك فعلا ستّة أيّامٍ في آب |

> ### ⚠️ الاثنان معا — ولا يُقرأ هذا «لا تضبط `NODE_ENV`»
>
> **يُضبطان كلاهما `production` في بيئة تشغيل الخادم** (`environment` في وظيفة
> Supervisor). ولكلٍّ منهما عملُه:
>
> - **`NODE_ENV`** يحرس أربعةَ أبوابٍ في الشيفرة (الصفُّ أعلاه) — أخطرُها
>   `devToken` في جواب `‎/api/auth/password/forgot`.
> - **`APP_ENV`** يحرس بابَ الديمو، ويُعلن البيئةَ في `‎/api/version`. وكان
>   ذلك يُقرأ من `VERCEL_ENV` تحقنه المنصّةُ بنفسها؛ **فزال بزوالها**، وخلَفه
>   اسمٌ محايدُ المضيف تضبطه أنت.
>
> **وموضعُ الضبط جزءٌ من الضبط:** `NODE_ENV=production` **في بيئة التشغيل
> وحدَها، لا في صَدَفة النشر ولا في بيئة المستخدم العامّة** — فسكربتُ النشر
> يشغّل `npm ci`، وهي معه **تُسقط حزمَ التطوير** التي يحتاجها البناء (`tsc`،
> `vite`، `prisma`) فينكسر النشر. بيئتان منفصلتان: التشغيلُ يحتاجه والبناءُ
> يمتنع عنه. **ولا يُترك غيرَ مضبوطٍ في التشغيل بحجّة أنّه يكسر البناء** —
> وهي القراءةُ الخاطئةُ التي كانت هذه الفقرةُ تحتملها.
>
> **والتحقّق بأمرٍ واحد** — ببريدٍ تملكه:
>
> ```bash
> curl -s -X POST https://www.wajeezacademy.com/api/auth/password/forgot \
>   -H 'Content-Type: application/json' -d '{"email":"<بريدك>"}'
> ```
>
> فإن ظهر `devToken` في الجواب فـ`NODE_ENV` غيرُ مضبوطٍ والبابُ مفتوحٌ الآن.

---

## ٤) كيف تعرف أنّ ما تراه هو الأحدث

**`‎/api/version` مفتوحٌ بلا مصادقة، وهو الجوابُ الوحيدُ الصادق:**

```
https://www.wajeezacademy.com/api/version
```

يردّ بجوابٍ عربيٍّ فيه: بصمةُ الالتزام العامل · الفرعُ · البيئةُ · **ووقتُ
البناء** · **ومصدرُ البصمة** · **وتسميةُ اللقطة المنشورة وهل هي من الالتزام
نفسِه**.

| ما تراه | ما يعنيه |
|---|---|
| الالتزامُ ليس آخرَ ما دفعتَه | النشرُ لم يكتمل، أو Varnish يقدّم قديما |
| «متطابقان: **لا**» | **اللقطةُ من التزامٍ آخر** — أي أنّ `catalog:publish` لم يعمل في آخر نشرة (القسم ٢) |
| «متطابقان: **نعم**» | الشيفرةُ واللقطةُ من التزامٍ واحد — النشرةُ اكتملت |
| **«الالتزام: null» و«مصدر_البصمة: مجهول»** | **البناءُ العاملُ بلا ختم**: `npm run build` لم يُشغَّل (فخطّافُ `prebuild` لم يجرِ)، أو أنّ الملفّات نُسخت نسخا بلا بناء. وحينها لا `‎/api/version` ولا غيرُه يعرف أيَّ نسخةٍ تعمل |
| «وقت_البناء» قديمٌ بأيّام | **النشرةُ لم تصل** — وهذا دليلٌ قاطعٌ حتّى لو جُهل الالتزام |
| لا يفتح أصلا | عمليّةُ Node متوقّفة |

### من أين تأتي البصمة

كان المصدرُ متغيّرا اسمُه `VERCEL_GIT_COMMIT_SHA` وحدَه. **وعلى Cloudways لا
وجودَ له**، فكان الجوابُ على الإنتاج الحيّ: «الالتزام: null · البيئة: محلية» —
امتناعٌ صادقٌ عن سؤالٍ صار أعمى، فلا يُعرف هل وصلت النشرةُ أم لا.

فالمصدرُ الآن ثلاثةٌ بترتيب الأسبقيّة (`server/build-stamp.ts`):

1. **بيئةُ المضيف** بأيّ اسمٍ أعلنها: `GIT_COMMIT_SHA` أو `VERCEL_GIT_COMMIT_SHA`
   أو `SOURCE_VERSION` أو `COMMIT_SHA`.
2. **ملفُّ `build-stamp.json`** يكتبه البناءُ نفسُه — يجري تلقائيّا قبل
   `npm run build` بخطّاف npm (`prebuild`)، ويقرأ التزامَه من نسخة Git
   الحاضرةِ على الخادم (والنشرُ على Cloudways سحبٌ من GitHub، فهي حاضرة).
3. لا شيء — فيقول «مجهول» ولا يخمّن.

> **ولا يُشغَّل `git` وقتَ التشغيل عمدا.** الخادمُ يخدم ما **بُني**، لا ما في
> القرص الآن: نسخةٌ سُحبت ولم يُعَد بناؤها تجعل `git rev-parse` يعلن التزاما لا
> يخدمه أحد — كذبةٌ في اللحظة التي تحتاج فيها الصدقَ أكثرَ ما تحتاجه.

> **والبصمةُ تدخل تسميةَ اللقطة كذلك** (`auto-<sha7>-<hash6>`)، وعليها يقوم
> «متطابقان». فبلا ختمٍ تخرج كلُّ لقطةٍ بالشكل الأعمى `auto-<hash12>` ويصير
> الجوابُ «لا يمكن الحكم» أبدا — وهو ما كان يقع على Cloudways.

> **وعرضُ هذا في شاشةٍ بندٌ مفتوح.** `‎/api/version` دقيقٌ **ولا يُستدعى من أيّ
> شاشةٍ في المنصّة**. فالسؤال «هل أرى الأحدث؟» لا جوابَ له إلّا بفتح هذا
> العنوان يدويّا.

---

## ٥) الدفع — Stripe

> الدليلُ العمليُّ في [`CONNECT_AR.md`](./CONNECT_AR.md).

- **عنوانُ الـwebhook:** `https://www.wajeezacademy.com/api/webhooks/payments/stripe`
  بحدث `checkout.session.completed`.
- المتغيّرات: `PAYMENT_DRIVER` · `PAYMENT_PUBLISHABLE_KEY` · `PAYMENT_SECRET_KEY` ·
  `PAYMENT_WEBHOOK_SECRET` — **لا `STRIPE_WEBHOOK_SECRET`، هذا الاسم لا يُقرأ**.
- `APP_URL` إلزاميّ: عليه تُبنى صفحةُ العودة بعد الدفع.
- القواعدُ الثابتة: لا بطاقاتٍ تُخزَّن · ورجوعُ المتصفّح ليس دليلَ دفع · والنجاحُ من
  الـwebhook وحدَه.
- **وفخٌّ يُذكر:** إن فُعِّل مزوّدٌ حقيقيٌّ **بلا مفتاحٍ سرّيّ**، يسقط النظامُ إلى
  المزوّد التجريبيِّ **صامتا** بينما تعرض الشاشةُ «صفحةُ دفعٍ آمنة».

---

## ٦) البريد

- **غيرُ موصولٍ اليوم** — ينتظر مفتاحَ Resend وحدَه.
- قبل الإرسال: `SPF` و`DKIM` و`DMARC` على النطاق، وإلّا وقعت الرسائلُ في المهملات.
- **وأثرُ غيابه ظاهرٌ للمستخدم:** شاشةُ ما بعد التسجيل تقول «أرسلنا رابطَ تحقّقٍ
  إلى بريدك» ولم يُرسَل شيء. (بندٌ في تقرير ٥ سبتمبر.)

### والمسارُ نفسُه سليمٌ ومُثبَت

المزوّدُ **Resend** (طلبُ السحب ١٦، دُمج في ٥ سبتمبر). وجُرّب المسارُ من طرفه إلى
طرفه على قاعدةٍ حقيقيّةٍ وواجهةِ Resend وهميّةٍ محلّيّة:

```
قناةُ البريد  : موصولة · مفتاح: موجود · من: no-reply@wajeezacademy.com
قبل الدورة    : queued
خبرُ الدورة   : حُوِّل ١ إشعارا: وصل ١، وسقط ٠
بعد الدورة    : sent · أُرسلت · لا خطأ
وواجهةُ Resend: POST /emails · من: أكاديمية وجيز <no-reply@wajeezacademy.com>
                إلى: عنوانُ المتعلّم · الموضوع: «وثّق بريدك»
```

**ومسارُ الفشل مُجرَّبٌ كذلك** بمفتاحٍ مرفوض:

```
خبرُ الدورة  : حُوِّل ١ إشعارا: وصل ٠، وسقط ١
بعد الدورة   : failed · «رفض Resend بيانات الدخول — راجع مفتاح API»
```

**فلا مجهولَ في الشيفرة — المجهولُ مفتاحُ المزوّد وحدَه.**

### ما يلزم للربط

| المتغيّر | ملاحظة |
|---|---|
| `RESEND_API_KEY` | **وجودُه وحدَه يفعّل القناة** · يبدأ بـ`re_` |
| `RESEND_FROM_NAME` | «أكاديمية وجيز» افتراضا |
| `RESEND_FROM_EMAIL` | `no-reply@wajeezacademy.com` افتراضا |

> **ونطاقُ المُرسِل هو ما يُوثَّق في Resend وتُضبط له SPF/DKIM — لا نطاقُ
> الموقع.** ورسالةٌ من نطاقٍ غيرِ موثَّقٍ يرفضها Resend نفسُه.

وتُضبَط إمّا في متغيّرات البيئة على Cloudways، **وإمّا من `/admin/integrations`
بلا نشرةٍ أصلا** — والمتغيّرُ يغلب الشاشةَ حين يوجد. **ولا يُرسَل مفتاحٌ في
محادثة.**

> **وفرقٌ تشغيليٌّ يُذكر:** Resend واجهةُ HTTPS لا SMTP. فالخادمُ يحتاج خروجا إلى
> `api.resend.com` على المنفذ ٤٤٣ — وهو مفتوحٌ افتراضا، **بخلاف منافذ SMTP التي
> تحجبها كثيرٌ من الاستضافات**. وهذا وحدَه سببٌ كافٍ لتفضيل Resend هنا.

---

## ٧) ما لا يعمل على Cloudways بلا عملٍ إضافيّ

| | الحال |
|---|---|
| **المُشغِّلُ الخلفيّ** | كانت حقولُ الجدولة تُكتب ولا شيءَ ينفّذها: لا تذكيرَ يُرسَل، ولا محتوًى يُنشَر في وقته. **صار له خدمةٌ في `compose.prod.yml` ويُقلع مع أوّل نشرة — والقسم ١٠ يشرح ما تغيّر** |
| **رفعُ الملفّات** | وثائقُ المتقدّمين تُخزَّن **داخلَ PostgreSQL** بسقف ٤ ميغابايت. ورفعُ التسجيلات والموادّ **لم يعمل يوما** |
| **بيئةُ التجريب** | لا وجودَ لها |
| **المراقبة** | لا متتبّعَ أخطاءٍ ولا فحصَ حياة |
| **النسخُ الاحتياطيّة** | Cloudways يوفّر نسخا افتراضيّا — **والمطلوبُ إثباتُ الاستعادة لا وجودُ النسخة** |

---

## ٨) قائمةُ فحصٍ عند كلّ نشرة

- [ ] سكربتُ النشر يشغّل **الخمسةَ** في القسم ٢ (الترحيل · البناء · الاستيراد ·
      النشر · إعادةُ التشغيل)
- [ ] `VITE_SITE_ORIGIN` و`APP_URL` مضبوطان
- [ ] تفريغُ Varnish
- [ ] `‎/api/version` يعرض آخرَ التزامٍ و«متطابقان: نعم»
- [ ] فتحةٌ في نافذةٍ خاصّة تؤكّد ما تراه

## ٩) قائمةُ فحصٍ قبل الإطلاق للعموم

- [ ] البريدُ موصولٌ و`SPF/DKIM/DMARC` مضبوطة
- [ ] `PAYMENT_DRIVER` حقيقيٌّ ومفاتيحُه مضبوطة، والـwebhook مُختبَر
- [ ] `STORAGE_SECRET` مضبوطٌ صراحةً
- [ ] نسخةٌ احتياطيّةٌ **جُرّبت استعادتُها**
- [ ] المُشغِّلُ الخلفيُّ يعمل
- [ ] الأسعارُ والشعبُ مضبوطة
- [ ] حذفُ بوّابات المعاينة الداخليّة من `/auth`

---

## ١٠) المُشغِّلُ الخلفيّ — يعمل، وقناتُه المغلقةُ لم تعد تحرق طابورَه

**العاملُ سليمٌ ومُجرَّب.** شُغّلت دورةٌ كاملةٌ على قاعدةٍ حقيقيّةٍ في ٥ سبتمبر،
فأبلغت وظائفُه الستُّ كلُّها صحيحا:

```
dispatch_notifications · session_reminders · cohort_status_sync
publish_scheduled_changes · cleanup_expired · enforce_retention
```

### ⚠️ وكان لا يُشغَّل قبل وصل قناة البريد — والسببُ مُثبَتٌ بالتجربة لا مُستنتَج

طابورُ الإشعارات يمتلئ منذ بداية المنصّة ولم يُفرَّغ قطّ. وجُرّب في ٥ سبتمبر ما
يحدث لو شُغّل العاملُ والقناةُ مغلقة — إشعارٌ في الطابور، ودورةٌ واحدة:

| | |
|---|---|
| قبل الدورة | `queued` |
| خبرُ الدورة | «حُوِّل ١ إشعارا: وصل ٠، وسقط ١ (قناةُ البريد أو المزوّد)» |
| بعد الدورة | **`failed`** · محاولات ١ · «لا مزوّدَ مربوطا لقناة email» |
| **ما زال في الطابور** | **صفر** |
| الدورةُ الثانية | «لا إشعارَ في الطابور» |

**ووظيفةُ الإرسال تختار `status: 'queued'` وحدَه.** فما صار `failed` **لا تنظر
إليه ثانيةً أبدا**.

> أي أنّ تشغيلَ العامل اليوم **يُحوّل الطابورَ كلَّه إلى «فاشل» في دورةٍ واحدة**:
> كلُّ رابطِ توثيقٍ معلَّق، وكلُّ دعوةِ موظّف، وكلُّ إيصالٍ — يُحرق دفعةً واحدة.
>
> والتعافي ممكنٌ لكنّه **يدويٌّ صفّا صفّا** من `/admin/notifications` بزرّ «إعادة
> المحاولة»، وبسقف ثلاثِ محاولاتٍ لكلّ صفّ.

### ✅ والمانعُ رُفع بتغيير الشيفرة لا بانتظار البريد (٦ سبتمبر)

انتظارُ وصلِ البريد كان علاجا لعَرَضٍ لا لعلّة. **والعلّةُ أنّ الوظيفة كانت
تُحاول ما لا مزوّدَ له**: محاولةٌ فاشلةٌ لقناةٍ مغلقة ليست فشلَ إرسال، بل غيابَ
إرسال — ولا يجوز أن تُعلَّم `failed`.

فصارت `dispatchQueuedNotifications` **تسأل عن القناة قبل الصفّ**
(`liveChannels` في `notification.service.ts`): تختار من الطابور ما قناتُه
موصولةٌ الآن، وتترك الباقيَ `queued` كما هو، **وتقول عددَه في خبر الدورة** فلا
ينتظر صامتا:

> «لا إشعارَ في الطابور · و٧ ينتظر قناةً لم تُوصَل بعد»

وهذا يقلب الترتيب: **العاملُ يعمل الآن، والبريدُ يُوصَل متى شئت.** ويومَ يُضبط
`RESEND_API_KEY` تدخل رسائلُ البريد المنتظرةُ الطابورَ في الدورة التالية بلا
تدخّل — سليمةً بعدّاد محاولاتٍ صفر، لا محروقةً تُستعاد صفّا صفّا.

ومحروسٌ في موضعين: `server/tests/worker/jobs.test.ts` بقاعدةٍ حقيقيّة (ودورتان
لا واحدة، فالعطبُ تراكميّ)، ومُثبَتٌ بالطفرة — نُزع الشرطُ فاحمرّ، وأُعيد
فاخضرّ.

> ⚠️ **وحدُّه صريح:** يقيس القناةَ لا الصفّ. فمستخدمٌ بلا بريدٍ مسجَّلٍ يبقى
> صفُّه يُعلَّم `failed` — وهو صوابٌ: لا عنوانَ له اليوم ولا غدا.

### والتشغيل: حاويةٌ ثانيةٌ في المنظومة

خدمةُ `worker` في [`deploy/compose.prod.yml`](../deploy/compose.prod.yml) —
الصورةُ نفسُها (`wajeez-app:latest`) بأمرٍ آخر، فلا بناءَ ثانٍ ولا شيفرتان
تفترقان:

```yaml
worker:
  image: wajeez-app:latest
  command: ["npx", "tsx", "server/worker/index.ts"]
  environment: { WORKER_ENABLED: "on" }
```

ولا `ports` لها ولا حجمَ واجهة: لا تواجه الإنترنت، ووظائفُها الستُّ قاعدةٌ
خالصة. وفحصُ الصحّة مُطفأ — فحصُ الصورة يسأل `/api/version` ولا خادمَ ويبٍ هنا،
فحاويةٌ سليمةٌ كانت ستُعلَّم «غيرَ صحّيّة».

**وتُقلع مع أوّل `bash deploy/deploy.sh` بعد وصول هذا التغيير إلى الخادم.**
ولقراءة ما تعمل:

```bash
docker compose -f deploy/compose.prod.yml --env-file deploy/.env.production logs -f worker
```

ودورةٌ واحدةٌ يدويّةٌ للفحص قبل الالتزام بالحلقة — تعمل مرّةً وتخرج:

```bash
WORKER_ENABLED=on npm run worker:once
```

> **وعلمُ التشغيل مقصود:** بلا `WORKER_ENABLED=on` يخرج العاملُ فورا معلنا أنّه
> غيرُ مفعَّل. فلا يعمل بالخطأ، ولا عمليّتان تتزاحمان على الطابور نفسِه — وفوقه
> قفلٌ في القاعدة (`pg_advisory_lock`) يُخرج النسخةَ الثانيةَ بلا ضجيج.

> **وما لا يفعله:** لا رصدَ ولا تنبيه. سقوطُ الحاوية يُعيدها Docker
> (`restart: unless-stopped`)، أمّا وظيفةٌ تسقط في كلّ دورةٍ فسطرٌ في السجلّ
> لا يوقظ أحدا. وهو البندُ نفسُه المفتوحُ في §٢-جـ.
